@AGENTS.md

# Location Architecture Roadmap

Authoritative future-context roadmap for Beggars Map's map/location provider stack, based on the approved "Beggars Map Location Stack" architecture investigation (3 Sep 2026). This section is the durable plan; **live deployment status for any stage below is tracked in AGENTS.md, not here** — check AGENTS.md's "Deployment status" and "Location provenance (Stage 2A)" sections before assuming anything here has shipped to production.

The original investigation defined 3 implementation stages. Stage 2 was subsequently, deliberately split into **Stage 2A** and **Stage 2B** during implementation — that split is intentional, not a deviation from the original plan.

## Stage 1: OLA place-selection fix

- Preserve OLA `types` from place predictions.
- Prefer real POI types over `street_address` when resolving same-name candidates.
- Fix the demonstrated branch/coordinate ambiguity.
- Require explicit pin confirmation before submission.

This stage is the low-cost accuracy fix and must remain conceptually separate from later provider migration.

## Stage 2A: Location Provenance + Evidence Foundation

- Add location provenance and confidence fields.
- Track `location_source`, `location_confidence`, verification metadata, and provider place IDs.
- Record provenance across web, mobile, paste-link, and discovery-import paths.
- Protect verification fields from ordinary user self-assignment.
- Support admin provenance/audit behavior.

This stage deliberately establishes the plumbing only. It does **NOT** perform actual coordinate verification. Production deployment status must remain whatever is currently documented in AGENTS.md — do not assume production migration/deployment has occurred.

## Stage 2B: Coordinate Verification — FUTURE / DEFERRED

This is the actual verification portion of the original Stage 2.

- Use Google Places **selectively** to cross-check a newly submitted human/device coordinate — not as the coordinate source of record.
- The database's human-confirmed/user-submitted coordinate remains canonical.
- Store Google `place_id`, not Google's latitude/longitude.
- Use independent-source agreement and distance gating to determine confidence.
- Ambiguous/distant matches must be flagged for human/admin review rather than automatically correcting coordinates.
- Admin review/backfill should be introduced only when this stage is explicitly approved.
- Before implementation, re-evaluate Google API/server-side key architecture, pricing, licensing, and the actual production data/requirements at that time.

**Do not implement Stage 2B now unless explicitly instructed.**

## Stage 3: Web MapLibre Migration — FUTURE / SCALE-TRIGGERED

- Move web map rendering from Google Maps JS to MapLibre.
- Mobile already uses MapLibre.
- Keep the tile provider swappable.
- OLA can remain the current base-map source while a future self-hosted PMTiles architecture is considered.

This stage is primarily driven by Google Maps rendering usage/cost approaching the relevant free-tier limit, not by a fixed calendar date or arbitrary user count.

**Do not start this migration prematurely.**

## Architectural principles that must survive all stages

- Our database is the canonical listing coordinate store.
- A provider's search/autocomplete result is a hint, not automatically truth.
- Never silently auto-correct a coordinate from provider disagreement.
- Google Places is for identity/verification, not permanent coordinate storage.
- Never store Google Places latitude/longitude permanently.
- Do not combine Google Places content with a non-Google map in a way prohibited by Google's terms.
- OLA remains useful for everyday India-focused search/labels/base tiles while its known coordinate-quality limitations are handled.
- OSM public infrastructure must not be treated as production infrastructure; self-hosting/buying appropriate OSM-derived infrastructure is a separate consideration.
- Degrade rather than fabricate a location when a provider fails.

## Sequencing rule

Stage 2A is complete foundation work. Stage 2B and Stage 3 are future stages and must remain separately scoped. Do not infer missing requirements or begin either stage without explicit approval.

# Community Reviews + Listing Corrections (Phase 2 + Phase 3) — LIVE IN PRODUCTION (deployed 2026-09-09)

Built (`a161751`), then deployed to production end to end the same day: migrations `0022`-`0024` applied via `db query --linked` (each wrapped in a single atomic `DO` block — the same safety pattern `0009` established, so a new table is never briefly exposed without RLS already enabled; never a bare `db push`, see AGENTS.md's migration-ledger caveats), and the `admin-corrections` Edge Function deployed and ACTIVE. **Confirmed genuinely live**: a real anonymous site visitor submitted a real review ("Must visit" on Shivaji Military Hotel) shortly after deployment, with no prompting — the first organic, unprompted evidence this feature works end-to-end in production, not just in testing. As of this writing, production holds 1 real review and 0 corrections (0 pending/approved/rejected).

## What this is

Clicking "Review" on a listing (list row or map popup, same as before) now opens a two-page overlay instead of the old read-only note/rating display:

- **Page 1 (default, read-only)** — "From the listing" (the creator's own note, or the price if there's no note — never blank), "Your review" (only if the current anonymous session already has one), "Reviews from others", and one bottom button: **"Add Review"** if the session has no review yet, **"Edit Review"** if it does.
- **Page 2 (opened by that button)** — Restaurant name / Dishes & prices / Location, each shown **read-only with an "Edit X" toggle** (editing reveals an inline editor with its own Save/Cancel; Save only stages the value locally, nothing is written yet), then Your rating / Your review / Photos (always directly editable, no toggle), then one **"Submit Review"** button. A "← Back" link returns to page 1 without submitting. Both pages share one unchanged header (restaurant name + close button).

Submitting splits into two independent write paths, by design:
- **Community content** (rating, review text, photos) → always an auto-published `listing_reviews` upsert (one row per listing per anonymous session, editable by its own owner, never overwritten by another user).
- **Canonical corrections** (a *changed* name/dishes/location — unchanged fields create no row at all) → a `listing_corrections` row per changed field, `status: 'pending'`. **Nothing here ever writes to `listings` directly** — the public client has no RLS path to do that; the only way a correction reaches the canonical row is the new admin Corrections queue's **approve** action, via the `admin-corrections` Edge Function running as `service_role`.

## Key files

- Schema: `supabase/migrations/0022_listing_reviews.sql` (`listing_reviews`, `listing_review_photos`), `0023_listing_corrections.sql` (`listing_corrections`), `0024_audit_log_correction_actions.sql` (widens `admin_audit_log`'s `action`/`target_type` CHECK constraints for `approve_correction`/`reject_correction`/`delete_review` and `listing_correction`/`listing_review`).
- Backend: `supabase/functions/admin-corrections/index.ts` (list/approve/reject/deleteReview), `_shared/listingActions.ts`'s new `applyListingCorrection`/`rejectListingCorrection` (the only code path that ever writes a correction into `listings`, with full before/after audit logging), `_shared/adminAuth.ts` (widened `AuditAction`/target-type unions).
- Frontend: `web/src/components/ReviewOverlay.tsx` (the two-page overlay described above — this is where almost all the new UI logic lives), `web/src/components/DishPriceRows.tsx` (extracted from `AddListingModal.tsx` for reuse in the dishes-editing section), `web/src/lib/reviews.ts` / `reviewValidation.ts` / `corrections.ts` (data-access + pure validation).
- Admin: `web/src/admin/views/CorrectionsQueue.tsx` (new nav tab — list pending corrections with a current-vs-proposed diff, Approve, or Reject with a required reason).
- **`AddListingModal.tsx` is untouched** — after an earlier pass tried reusing it in a "review mode" and was explicitly reverted, it's back to being create-mode-only, byte-for-byte its pre-this-feature self (the one harmless diff is the `DishPriceRows` extraction, a pure markup move).

## Map-popup Edit Location limitation — REMOVED (2026-09-09, commit `726bd8c`), LIVE IN PRODUCTION

"Edit location" used to only appear when `ReviewOverlay` was opened from the list row's own Review link, not from the map popup's — the popup's `ListingDetailModal.tsx` unmounted while a location was being picked (`hidePopup` in `MapView.tsx`), which would have silently discarded an in-progress correction form. **Fixed, not by touching that popup-unmount logic at all**: `ListingDetailModal.tsx` no longer owns its own `ReviewOverlay` instance. Its "Review" button now calls a new `onOpenReview` prop (threaded through `MapView.tsx`) that App.tsx wires directly to `setReviewListingId` — the exact same state that already drives the list row's own `ReviewOverlay`. Both entry points now render the *same* single `ReviewOverlay` instance, owned by `App.tsx`, which never unmounts during a location pick (it already used the existing `hidden`-fade-not-unmount treatment `pickingLocation` already gave it). The popup itself still hides/unmounts during a pick exactly as before — that's fine now, since the overlay showing the review no longer lives inside it. Name and dishes corrections were already identical from both entry points and are unaffected. **Verified live**, not just locally: a fresh, uncached browser session against `www.beggarsmap.com` itself confirmed map-pin → Review → Edit location → pick → Save works end to end, with staged name/dishes/rating/text/photo all surviving the location pick.

## Verification done — local and production

Full `npm test` (350 passing, excluding the pre-existing, unrelated Discovery Workbench local-state-collision issue documented under "Discovery Workbench — current workflow and safeguards" below), `tsc --noEmit` and `npm run build` clean, plus repeated real-browser Playwright passes covering: per-field edit/save/cancel, the ₹30-₹100 dish validation still enforced on Save, the real map-picking round-trip from both entry points, only-changed-fields-create-corrections (verified directly against the DB), the canonical `listings` row staying untouched after submit, two independent anonymous sessions never overwriting each other's review, Add Listing's own create flow being unaffected, and no CSS overflow on desktop or 390px mobile. Beyond local testing, this was also smoke-tested directly against **production** (real anon-key writes to a disposable test review/correction, cleaned up afterward; one `admin_audit_log` row from that pass is permanently retained — that table is append-only/immutable by design, see `0013`) and confirmed via a live, fresh-browser walkthrough of the deployed site.

## Deployed to production — what's left is Admin v2 UI, not Phase 2+3 itself

Migrations `0022`-`0024` are applied, `admin-corrections` is deployed and ACTIVE. What remained after that — making these capabilities actually usable/manageable from the admin panel — became its own phase; see "Phase 5: Admin Corrections & Moderation" below. No real interactive walkthrough of the admin Corrections/Reviews UI by an actual logged-in human has happened yet (needs a real Google OAuth session, same limitation as every other Admin v2 feature before it) — everything so far has been verified via a locally-minted admin JWT (local Supabase stack only) plus direct read-only SQL replication against production.

# Phase 5: Admin Corrections & Moderation — LIVE IN PRODUCTION (commit `baa26d6`, deployed 2026-09-10)

Makes every Phase 2+3 backend capability reachable from the admin panel, closing gaps a full audit turned up: several backend capabilities existed with no UI, `deleteReview` was fully built server-side with zero call sites, and the Audit Log had an active bug mislabeling every `listing_correction`/`listing_review` row as `"report · {id}"` (migration `0024` widened the vocabulary; `AuditLog.tsx`'s Target-column renderer never learned about it).

- **Audit Log fix**: correct labeling for `listing_correction`/`listing_review` rows; added the missing action/target-type filter options (also closed a pre-existing, unrelated gap — `mark_reviewed`/`mark_unreviewed` were missing too, since migration `0014`).
- **Corrections Queue**: status filter (pending/approved/rejected/all — the backend's `status` param already supported this, the UI just never called it) and `correction_type` filter; shows the submitter's display name (joined server-side); shows `reviewed_at`/`reviewed_by`/`rejection_reason` for decided corrections.
- **Review moderation (new)**: a global "Reviews" admin tab plus a per-listing Reviews section in `ListingDetail`, both backed by a new `listReviews` action on `admin-corrections`. `deleteReview` now requires a reason, recorded in the audit row's existing `request_metadata` jsonb (no schema change).
- **`ListingDetail`**: a listing's own correction-approval and review-deletion history now appears in "History for this listing" — previously invisible there, since those audit rows are filed under the correction/review's own id, not the listing's (`admin-listings`' `get` action now runs two extra targeted queries to find them).
- **Dashboard**: pending-corrections and total-reviews tiles.
- **Deliberately excluded, confirmed with the user**: location-provenance fields stay read-only (adjacent Stage 2A work, not part of this phase); no bulk approve/reject.
- No migration — every gap closed was a stale frontend type/UI or new query logic against tables that already existed.

**Deployed functions**: `admin-corrections` v1→v2, `admin-listings` v7→v8, `admin-dashboard` v5→v6, all ACTIVE. Verified via the same locally-minted-JWT + production SQL-replication method as Phase 2+3's own deployment (real Google OAuth still isn't available in this environment) — auth-boundary (401/no-header) confirmed against the real deployed functions; every new/modified query's exact logic replicated against real production data with matching results. Before/after production counts (listings, hidden, reviews, corrections, Discovery Workbench batch 5) confirmed byte-identical across the whole deployment.

# Analytics: Plausible → GA4 + Cloudflare Web Analytics — LIVE IN PRODUCTION (commit `7603b55`, GA4 configured 2026-09-10)

Plausible was removed entirely (it had never been wired to a real account — see the old `index.html` comment it replaced) and replaced with Google Analytics 4 and Cloudflare Web Analytics, each loaded independently at runtime by `initAnalytics()` in `web/src/lib/analytics.ts` (called once from `main.tsx`), never as a static `<script>` tag in `index.html` — a static tag can't conditionally omit itself when its env var is unset, and the explicit requirement here was **never load a script with a blank/invented id**.

- `initAnalytics()` reads `VITE_GA4_MEASUREMENT_ID` and `VITE_CLOUDFLARE_BEACON_TOKEN` independently; each only injects its own official snippet (via `document.createElement`) when its own var is present and non-empty. Neither reads the other.
- `trackEvent()` keeps its exact pre-existing signature — every `App.tsx` call site (`'listing viewed'`, `'listing submitted'`, `'Add Listing opened'` ×3) needed zero changes. Internally it now calls `window.gtag?.('event', ...)` instead of `window.plausible?.(...)`.
- `web/vercel.json`'s CSP (Report-Only) gained `googletagmanager.com`/`static.cloudflareinsights.com` (script-src) and `google-analytics.com`/`cloudflareinsights.com` (connect-src).
- **GA4 is live and verified in production**: `VITE_GA4_MEASUREMENT_ID=G-79HX172B78` was added to Vercel's Production env vars (this environment had no Vercel session to do it directly — confirmed via `vercel whoami` reporting logged out with no `VERCEL_TOKEN`; the user added it and triggered the deploy manually) and confirmed live via a real, fresh-browser load of `www.beggarsmap.com`: exactly one `googletagmanager.com/gtag/js` script, `gtag('config', 'G-79HX172B78')` present in `dataLayer`, and zero Plausible references anywhere.
- **Cloudflare Web Analytics is NOT configured** — `VITE_CLOUDFLARE_BEACON_TOKEN` has never been set anywhere (not even a placeholder value beyond `.env.example`), so `initAnalytics()` correctly skips it entirely. Add the real token to Vercel's env vars whenever that account exists; no code change needed.
- `resolve-maps-link` (an unrelated Edge Function) was **not** touched by this work — mentioned only because it was redeployed later the same week for the unrelated location-accuracy work below; don't conflate the two.

# Location Accuracy: Map Search + POI-Tap + Paste-Link (2026-09-10 → 2026-09-11)

A multi-part pass triggered by a real user report: searching Beggars Map for **"Vigneshwara Tiffens"** (a real Nallurahalli/Whitefield restaurant) resolved to a **Hyderabad** location ~500-570 km away, and pasting the restaurant's own Google Maps share link produced a **Banashankari**-area coordinate instead. This is the concrete, shipped-and-in-progress work behind Stage 1 (OLA place-selection fix) and Stage 2A (location provenance) in the Roadmap above — not a parallel initiative.

## Root causes found, in order of discovery

1. **`extractGoogleCoordsFromUrl`'s regex priority was wrong.** A resolved Google Maps place URL can carry *both* `@lat,lng` (the map's viewport center — can be a wide, zoomed-out regional point) and `!3d lat!4d lng` (the place's own precise geocoded coordinate) in the same string. Checking `@` first silently returned the viewport as if it were the listing's location. Fixed by reordering to `!3d!4d` → `?q=` → `@` (both `web/src/lib/extractGoogleCoords.ts` and the mobile mirror `src/lib/extractGoogleCoords.ts`).
2. **OLA's own autocomplete has a genuine coverage gap, not a ranking bug** — biasing a query at the restaurant's *own* coordinates still returned zero Whitefield results (4 of 5 predictions were in Hyderabad). This is why the map-pin/search-suggestion fix below is a *sanity guard*, not a fix for OLA's index itself.
3. **The suggestion dropdown had its own, separate instance of the bug** — the pin-resolution fix alone didn't touch what the live-typing dropdown displayed, so cross-city results kept appearing there even after the pin itself was fixed.
4. **Directions links (`/maps/dir/...`) can silently produce a wrong coordinate** — their `@lat,lng` is the *route's* viewport, not either endpoint; one captured example carried the real destination coordinate via a completely different, undocumented `!1d!2d` convention that no parser here reads.
5. **The pre-existing `share.google` → OLA-text-search fallback in `resolve-maps-link` was itself a guess** — predating this pass's "never guess a restaurant's location" rule. Removed outright; a `share.google` link that resolves to a plain Search page now fails safely instead.
6. **Google's Maps JavaScript API base-map POI icons are a curated subset of the full Places index, not a mirror of it** — confirmed via Google's own developer docs/discussions ("no algorithm or rhyme or reason for which icons are shown"). A restaurant fully visible on consumer Google Maps can legitimately never render as a tappable icon on our embedded map, at any zoom. This means **POI-tap-to-select (below) cannot be a complete solution by itself** — confirmed live: this exact restaurant never rendered as an icon on our map.
7. **Some real Google Maps share links resolve to a URL with no coordinate anywhere in it** — only an opaque CID/place ID (`!1s<hex>:<hex>`, no `@`/`?q=`/`!3d!4d`). Resolving a bare CID to coordinates has no free path — only the Google Places API (Place Details) can do it, and adding that API was explicitly ruled out this pass.

## What shipped — commit `bb4e2bb`

`fix: keep map search within current city context` — the `!3d!4d`→`?q=`→`@` reorder (item 1), plus a new **geographic sanity guard** in `web/src/lib/placeRanking.ts`: `filterByBiasDistance()`/`MAX_BIAS_DISTANCE_KM = 50` excludes any OLA candidate more than 50 km from the search bias point *before* name-ranking runs. Deliberately a coarse sanity check, not a proximity-ranking system — the existing `NAME_TIE_BAND`/type-rank tie-break logic (needed for the unrelated "Juicy Spot" case, where the *correct* candidate is the farthest of four identically-named ones within ~8 km) is completely untouched, since 50 km never overlaps that case. Bengaluru is currently the only enabled city, so this protects that one context; **a future second city needs real city-selection state/config, not a wider radius here** — checked and confirmed no such state exists in `App.tsx` today (the city `<select>` is cosmetic: hardcoded value, no-op `onChange`, every option but Bengaluru disabled).

## What shipped — commit `ee6eb2c`

`feat: keep map search within current city + fix paste-link safety + POI tap-to-select` — three things bundled into one commit/deploy pass:

**Step 0 (the remaining safety fixes)**:
- The city-context guard from `bb4e2bb` was also applied to the live-typing **suggestion dropdown** (item 3 above) — `App.tsx`'s `resolveAreaMatches` now filters `placeResults` through `filterByBiasDistance` too, not just the pin-resolution candidate.
- **Directions-link guard** (item 4): `extractGoogleCoordsFromUrl` now refuses outright (`return null`) the moment a URL contains `/maps/dir/`, before any pattern match — both platform copies.
- **`share.google`-OLA-guess removed** (item 5): `supabase/functions/resolve-maps-link/index.ts`'s entire OLA-text-search branch and its `bestPlaceMatch`/`predictionsToPoints` import are gone; the function now unconditionally returns `{ finalUrl }` for every resolved redirect, letting a coordinate-less Search-page URL fail safely on the client instead.
- **Redeployed live**: `resolve-maps-link` → version 9, confirmed via a real smoke test against the actual `maps.app.goo.gl` short link (normal redirect resolution intact).

**Step 1 (POI-tap-to-select — new capability)**: while picking a location for a new listing, Google's own base-map POI icons become tappable.
- `MapView.tsx`: new `poiSelectable` prop, live-toggles `map.setOptions({ clickableIcons })` in its own effect (never touches the map-init effect's own deps, so the map is never torn down when picking mode toggles). The `'click'` handler is widened to `MapMouseEvent | IconMouseEvent`; when `placeId` exists *and* `poiSelectableRef.current` is true, calls `e.stop()` (suppresses Google's native info card/recenter — the exact behavior `clickableIcons: false` was originally added to avoid) and passes the placeId through. Two independent gates (the live `clickableIcons` toggle, and this ref check) both have to agree, so a POI tap is impossible outside picking mode — verified via a synthetic `IconMouseEvent` fired at the real, live-registered click handler.
- `App.tsx` → `AddListingModal.tsx`: the placeId threads through `searchPin` → `pickedLocation` → `locationPlaceId`, setting `location_source = 'google'` (instead of the generic `'user_pin'`) and including `provider_place_ids: { google: <id> }` in the insert payload when present.
- **Confirmed free**: reading `IconMouseEvent.placeId` is not a billed API call — it's a property already present on data the map tile load already paid for; only a *separate* Place Details call would bill, and none is made here (confirmed against Google's own docs).
- **`web/src/lib/listings.ts`**: `CreateListingInput` gained an optional `provider_place_ids` field.

**Vercel deploy status**: this environment has never had Vercel credentials (`vercel whoami` confirmed logged out, no token) — pushing `bb4e2bb`/`ee6eb2c` to `origin/main` does not by itself mean the live site is running this code. The user was told exactly this each time; **whether either commit has actually been deployed to `www.beggarsmap.com` is unconfirmed as of this writing** — verify the live bundle before assuming either fix (or POI-tap) is actually live, the same standing caution AGENTS.md already documents for this repo's history of stalled deploys.

## The POI-tap limitation, confirmed live (no code change — investigation only)

After `ee6eb2c` shipped, the same restaurant was confirmed live on **consumer** Google Maps but still did not render as a tappable icon on Beggars Map's own embedded map — proving item 6 above in production, not just in theory. Also confirmed: this is **not** the sandbox's known `localhost` API-key-referrer-restriction issue (production's key is authorized — zero `RefererNotAllowedMapError` on a live fetch of `www.beggarsmap.com`); it's the inherent base-map POI curation limitation. A full options comparison (accuracy/API/billing/complexity) concluded the only *complete* remaining paths without a paid Places API are manual: "Pick on map" (already built) and copying an exact coordinate from Google Maps' own "What's here?"/long-press feature (not yet supported by the paste box at the time).

## Location provenance: client INSERT can now keep honest values — migration `0025`, **NOT YET COMMITTED, NOT applied to production**

`supabase/migrations/0025_location_provenance_insert_persistence.sql` amends `lock_listing_location_fields()` (0015) so a client INSERT can keep a genuinely-known, non-escalating provenance claim instead of 0015's original "always collapse to `unknown`":
- `location_source`: passes through only if `user_pin`/`device_gps`/`ola`/`google`/`unknown` — `admin`/`import` (or anything else) still forced to `unknown`.
- `location_confidence`: a submitted `human_confirmed` is still forced to `unknown`; nothing else changes.
- `location_verified_at`/`location_verified_by`: still unconditionally `null` on every client INSERT — completely unchanged.
- `provider_place_ids`: passes through only when it validates as a genuine object-of-strings (new `is_valid_provider_place_ids()` SQL function, mirroring the existing JS validator's exact rule); anything else forced to `{}`.
- UPDATE-time protection is byte-identical to 0015's — untouched.

This is what finally lets a POI-tap's captured `provider_place_ids.google` actually reach storage; until this applies anywhere beyond local, POI-tap's identity capture is verified only up to the database boundary. **Verified live against the local stack only** (real anonymous session, real INSERT/UPDATE attempts, service-role readback — anon/authenticated only hold a column-level SELECT grant per `0017` that excludes every provenance column, so verification always reads back via service_role). Found and fixed one genuinely stale expectation in `tests/rls.test.mjs`'s own C-1 forged-fields test while verifying this (it assumed *any* client-submitted `provider_place_ids` must revert — updated to use an actually-invalid shape for that assertion, plus a new positive-case test proving a valid one now legitimately persists).

## Paste-link: simplified to coordinates-only — COMMITTED

Two passes here, and the second one supersedes most of the first:

**First pass** (still present in the working tree, now largely superseded by the second): `web/src/lib/extractGoogleCoords.ts` (+ mobile mirror) gained `extractLatLngFromText()` (a bare `lat, lng` paste, whole-string-anchored so it can never misfire against a real URL, range-validated, tagged `user_pin`) and `extractPlaceNameFromUrl()` (pulls a name/address out of a coordinate-less place URL's path — verified against the real captured CID-only Nallurahalli URL). `web/src/lib/googleMapsLink.ts` was rewritten to try the bare-coordinate check first, then short-link resolution (now also matching `g.co`, not just `goo.gl`/`share.google`), returning a discriminated `{ok:true,...} | {ok:false, reason, name?, address?}` result. `resolve-maps-link`'s server-side allowlist also gained `g.co` — **this specific change has not been redeployed**, so the live v9 function does not yet recognize `g.co` links.

**Second pass, after seeing the actual UI** (screenshot showed real confusion from the URL-resolution path's complexity): `AddListingModal.tsx`'s paste box was simplified to **coordinates-only** — "Paste link" became **"Paste coordinates from Google Maps"**, and `usePastedCoordinates()` (renamed from `useMapsLink`) now calls `extractLatLngFromText` directly and synchronously, with no network call, no short-link resolution, no Places API, no OLA, no guessing of any kind. The now-pointless async "…" busy state (`parsingLink`) was removed. **As a direct consequence, `googleMapsLink.ts`'s rewritten `parseGoogleMapsUrl` (URL resolution, `g.co`, name/address extraction, the three-reason error split) is no longer called by web's UI at all** — `web/src/lib/googleMapsLink.ts` itself was deliberately left **uncommitted** (see "Parked" below): nothing imports it any more, so its own content, committed or not, has no effect on the running app. `extractPlaceNameFromUrl` (in `extractGoogleCoords.ts`, which **is** committed alongside `extractLatLngFromText`) is likewise orphaned/unused, kept only because deleting it wasn't asked for. Verified live: pasting `12.9723, 77.7345` produces an exact pin instantly, reverse-geocoded to "Nallurahalli Road, Whitefield."

## Add Listing location UX polish (2026-09-11) — COMMITTED

Two follow-up passes on the same `AddListingModal.tsx` location section, both reacting to real UI feedback after the coordinates-only simplification above shipped:

- **Location-tabs redesign, fixing a real (not just cosmetic) state bug.** "Use current location" used to render with a permanent solid-pink `active` fill from the moment the modal opened — `locationMode` defaults to `'current'`, and critically is *also* reset to `'current'` by the `pickedLocation` effect once "Pick on map" resolves, so the button stayed lit even after the user picked a location the other way. Since the separate "Pinned ✓" banner already shows whichever location actually got confirmed, this `active` styling was redundant at best and misleading at worst. Fixed: "Use current location" and "Pick on map" no longer carry any `active` state; "Paste coordinates from Google Maps" keeps one, but softened from a solid fill to a light pink tint (`var(--pink-accent-soft)`), since for that one button it legitimately means "this section is expanded," not "this is the confirmed source." All three buttons were also switched from wrapping pills (which left the long "Paste coordinates…" label stranded alone on its own line) to a consistent full-width stacked layout, matching every other button in this modal (`10px` radius, not a pill).
- **Coordinate-help ⓘ popover.** A small circular ⓘ button beside "Paste coordinates from Google Maps" opens a compact popover explaining how to actually get a coordinate off Google Maps — desktop (right-click the exact spot, click the coordinates in the menu to copy) and mobile (long-press the exact spot; the coordinates can appear at the *top or bottom* of the screen depending on device — copy them, then come back and paste them here, since they never fill in automatically), plus a worked example (`12.9723, 77.7345`). Implementation notes worth keeping:
  - Reuses `ListingDetailModal.tsx`'s existing report-popover interaction pattern exactly (a wrap `ref` + a `mousedown`-outside-click effect) rather than inventing a new one.
  - The ⓘ button is positioned *absolutely* on top of the "Paste coordinates" button's own right edge (with `padding-right` on the button reserving the visual space), not laid out beside it in a flex row — an earlier version shared row width between the button and the icon, which made that one button narrower than its two siblings. All three are now pixel-identical widths.
  - The popover measures real available space via `getBoundingClientRect()` against `window.innerHeight` at the moment it opens and flips to open **upward** (`.location-info-popover-top`) instead of downward whenever there isn't roughly 190px of room below — this modal's body scrolls and the trigger sits well down the form, so a fixed "always opens below" popover was observed running past the visible viewport and covering the Post listing button entirely.

## A real, unrelated CSS bug fixed in the same pass — COMMITTED

The Add Listing modal's header (`Add a listing` + ✕) visually overlapped with scrolled dish rows once enough "+ Add more" rows were added. Root cause: `.modal-header` had `position: sticky; top: 0` but no `z-index` — sticky alone doesn't guarantee paint order, so `.modal-body`'s later-in-DOM content painted *over* the header the instant scrolled content overlapped it. Fixed with one line, `z-index: 1` on `.modal-header` (`web/src/styles.css`). Verified with real screenshots across the full scroll range (0/25/50/75/100%) after adding 6 extra dish rows — clean at every position.

# Mobile Web: Search Camera Zoom, Keyboard, and Enter-Key Hint (2026-09-11)

**Important scoping correction, worth remembering:** a report of "map freezes / keyboard stays open / searching a locality zooms out to India-level / Vigneshwara Tiffens still resolves to the wrong city / nearby listings don't show" was initially — and incorrectly — diagnosed and partially "fixed" against the **React Native mobile app** (`src/`). The actual report was about **mobile web** (`beggarsmap.com` opened in a phone's browser), a completely different codebase. The RN-app diagnostic and its fixes were caught before being committed and were **deliberately left uncommitted** — see "Parked" below; none of it shipped, and the RN app itself is untouched by this section. Everything below is the corrected, actual mobile-web fix.

## Root cause: `MapView.tsx`'s search-camera `fitBounds` padding wasn't breakpoint-aware

`MapView.tsx`'s `searchFocus` effect (the camera move triggered by an executed search — Whitefield, a named restaurant, Vigneshwara Tiffens, all of it) called `map.fitBounds(bounds, { top: 100, right: 380, bottom: 60, left: 60 })` unconditionally, on every breakpoint. That padding was correctly sized for desktop's floating list panel (~340-460px wide, docked on the right) — but mobile portrait has no such panel at all (it's a bottom sheet, per AGENTS.md's own "Web mobile portrait only" section), and `right: 380` alone reserves more width than a real phone viewport even has (~360-430px). With `left: 60` added, `fitBounds` was left with zero or negative usable width to fit anything into. **Confirmed live**, not just in theory: the exact `right:380` bytes were found in production's own deployed bundle (`main-*.js`, fetched and grepped directly from `www.beggarsmap.com`) before this fix — Google Maps degrades to an extremely low (country-level) zoom when asked to satisfy an impossible padding constraint, which is exactly what "zooms out to India" looked like. The pre-existing over-zoom guard (`if (zoom > 15) setZoom(15)`) only ever protected against zooming in too far — nothing protected the opposite direction, so this failure mode was completely unguarded.

The area-search logic itself (`resolveAreaMatches`, `AREA_MATCH_RADIUS_KM`, the nearby-Beggars-Map-listings lookup) and the 50km geographic sanity guard (`filterByBiasDistance`, from `bb4e2bb` above) were both already correct and confirmed live in production (same bundle-inspection method — the guard's `haversineKm` constant `6371` appears twice in the live bundle, once for it and once for the pre-existing `distanceKm`, matching a clean local build byte-for-byte). Neither needed a fix. The *visible symptom* — a correctly-resolved point still rendering as a country-level zoom-out — was purely this one padding bug, since even a single resolved point goes through `fitBounds` rather than a plain `panTo` (see the effect's own comment for why: "a single point becomes a zero-area bounds").

**Fix** (`web/src/components/MapView.tsx`): the effect now checks `window.matchMedia('(max-width: 720px) and (orientation: portrait)')` — the same breakpoint constant `App.tsx` already uses everywhere else, duplicated locally rather than threading a new prop through — and switches to `{ top: 90, right: 40, bottom: 140, left: 40 }` on mobile portrait. Desktop/tablet/landscape padding is byte-for-byte unchanged.

## Keyboard fixes

- **Dismiss on search** (`web/src/App.tsx`): the search `<input>`'s Enter handler now calls `e.currentTarget.blur()` right after `executeSearch(query)` fires. Previously nothing ever blurred this input, so the on-screen keyboard stayed open after submitting a search, covering roughly half the screen.
- **`enterKeyHint="search"`** (`web/src/App.tsx`): the on-screen keyboard's Enter key now shows "Search" instead of a generic return glyph — the direct web equivalent of a native app's `returnKeyType`.
- **iOS Safari auto-zoom fix** (`web/src/styles.css`): `.search-input-inline`'s font-size was `14px` (shared phone rule) and `12px` (landscape-phone rule) — both under the 16px threshold at which iOS Safari auto-zooms the whole page when a text input receives focus, which read as "the page zooms when I tap search." Both floored to `16px`; padding on both left unchanged, so the layout only grows by exactly what the larger text requires.

## Verification

`cd web && npx tsc --noEmit` and `npm run build` both clean after every change in this section. `npm test` (full repo suite): consistently ~403 passing regardless of run (one run counted 447 tests/44 fail, another 419/16 fail — the difference is `adminAuth.test.mjs`/`discoveryWorkbench.test.mjs` self-skipping when the local Docker stack isn't reachable at that moment, not a regression; the remaining failures are always exactly `workbenchSync.test.mjs`'s own pre-existing, known, local-state-dependent set) — zero regressions from any change in this section, confirmed by name and count before and after. Verified the live production bundle directly (fetched, not assumed) both before this fix (to confirm the bug was genuinely deployed, not a stale-build artifact) and after (see below): committed as `8c9b35e`/`eec7f76`/`0164dc6`, pushed, and **confirmed LIVE in production** within roughly a minute of pushing — the deployed `main-*.js`/`main-*.css` were fetched directly from `www.beggarsmap.com` and grepped for `right:40`/`bottom:140` (mobile padding), `right:380` (desktop padding, unchanged), `enterKeyHint`, `.blur()`, `location-info-popover`/`location-tab-with-info`, the `12.9723, 77.7345` example string, `.search-input-inline{...font-size:16px` (both phone rules), and `.modal-header{...z-index:1` — every one present. Homepage and `/admin.html` both still return `200`.

No device/emulator was available in this environment for a real on-screen tap-through; all of the above is code-level, bundle-inspection, and build/test verified, not hand-tested on a physical phone.

## Current, honest state of this whole pass (as of this writing)

- **Committed and pushed**: `7603b55` (analytics), `bb4e2bb` (city-context guard), `ee6eb2c` (dropdown fix + Directions guard + OLA-removal + POI-tap). GA4 confirmed live in production; whether `bb4e2bb`/`ee6eb2c`'s *code* has reached the live site is unconfirmed (no Vercel access from this environment either time) — see the mobile-web section above, which independently reconfirmed both are in fact live via direct bundle inspection.
- **Deployed**: `resolve-maps-link` Edge Function is live at version 9 (includes the Directions-guard-adjacent OLA removal; does **not** include the later `g.co` addition, since that was written after this deploy and was never committed — see "Parked" below).
- **Not committed, sitting in the working tree**: migration `0025` + its `tests/rls.test.mjs`/`tests/locationProvenance.test.mjs` coverage; `web/src/lib/googleMapsLink.ts`'s rewritten-but-orphaned `parseGoogleMapsUrl`; the `resolve-maps-link` `g.co` allowlist addition. See "Parked" below for the full, current list — this bullet is deliberately not repeated there in full.
- **Full verification for every committed piece**: `npm test`, `tsc --noEmit`, and `npm run build` all clean at each step — see each section's own "Verification" note above for exact counts.

# Add Listing: Photo Copy/Paste + Contextual Location Help (2026-09-11)

A follow-up UX pass on `AddListingModal.tsx` (and, for one piece, the full-screen map's own picking-location dialog in `App.tsx`), driven by direct user feedback across several rounds after the initial coordinate-help ⓘ popover shipped in the "Add Listing location UX polish" section above.

## Paste-coordinates button — real centering, not just visual

The ⓘ trigger beside "Paste coordinates from Google Maps" used to reserve its own `padding-right` and force `text-align: left` on that one button, so its label text didn't actually center the same way its two siblings ("Use current location"/"Pick on map") did — all three needed to be pixel-identical. Fixed by removing that override entirely: the button now inherits `.location-tabs .tab-button`'s styling completely unstyled, and the ⓘ button is a pure `position: absolute` overlay with zero reserved layout space, so its presence can never shift where the label text centers.

The ⓘ button itself was also too subtle to read as an information affordance — a faint white circle with a light gray border and gray glyph, indistinguishable from the white button it sat on except on hover. Switched to a solid `var(--pink-accent)` fill with white text and a small drop shadow (matching this app's `.primary-button` accent language), so it reads as "tap me for information" at a glance instead of only on hover.

## Photo copy/paste — a real Discovery-Workbench-style paste box, not an invisible one

Went through two iterations here, worth recording since the first one looked right on paper but didn't actually work as asked:

1. **First attempt**: a document-level native `paste` event listener, active for the modal's whole lifetime, so Ctrl+V anywhere in the form would add a photo with zero new UI. This technically worked for the keyboard shortcut, but delivered **no right-click → Paste option and no mobile long-press → Paste option** — browsers only offer those two in their native context/selection menus over an element they recognize as an actual text-editing surface (a real input/textarea, or `contentEditable`), and a plain focusable `<div>` (even with `tabIndex`) doesn't qualify, no matter how it's styled.
2. **Fixed version**: the paste target is now a small `contentEditable` box (`.photo-paste-box`, 56×56px to match the existing `.photo-thumb` size) placed directly beside any already-added photo thumbnails in `.photo-thumbs`, with "+ Add photos" moved to its own line below that row. Because it's genuinely `contentEditable`, right-click → Paste (desktop) and the long-press selection menu → Paste (mobile) both now appear over it, exactly mirroring the Discovery Workbench's own `discovery-photo-paste-box` pattern (`CandidateDetail.tsx`) — the explicit reference point for this feature. `handlePhotoPaste` calls `preventDefault()` so the browser's own contentEditable behavior (inserting the image as a real `<img>` node, or raw text) never actually lands inside the box — its real DOM content must always stay empty; the "Paste" hint text is a pure CSS `:empty::before`, and a small `onKeyDown` guard blocks ordinary typing (Ctrl+V/Cmd+V still passes through) so a stray click-and-type can't leave real text sitting in it.

Both the file-picker's `onChange` and the paste handler now funnel through one shared `addPhotoFiles()` — same `MAX_PHOTOS` limit, same preview generation, same everything else, regardless of how the File arrived. No new client-side type/size validation was added (matching the pre-existing file-picker path, which also has none — the storage bucket's own `0018` restrictions remain the actual enforcement layer).

## "Can't find the restaurant?" — moved to where it can actually help

This was first added as a static link inside `AddListingModal.tsx`'s own location section. That placement didn't survive contact with the real flow: the modal is `hidden` (not unmounted, but invisible) for the entire duration of "Pick on map," so a note living inside it could never be seen by someone actually stuck searching on the full-screen map — exactly the moment (the Vigneshwara Tiffens case) this is meant to help with.

**Moved to `App.tsx`'s existing `picking-dialog`** — the one-time acknowledgment card that already appears the instant "Pick on map" is clicked ("Tap the map, tap a restaurant shown on it, or search a location/landmark..."). A second paragraph was added underneath it, visually separated by a top divider (`.picking-dialog-fallback`, same convention as the coordinate-help popover's own `.location-info-example` divider):

> Can't find the restaurant? Search for a nearby landmark or area, then directly pinpoint the restaurant on the map and click "Add this place". You can also copy the coordinates from Google Maps and paste them in the Add Listing form's "Paste coordinates from Google Maps" option.

Deliberately reworded from an earlier draft that said "...and paste them here" — that phrasing only made sense when this lived inside the modal itself; now that it's shown on a different screen (the modal is hidden throughout), it explicitly points back to the Add Listing form and names the exact button by its real label. Scoped to the `pickingSource !== 'current-location'` branch only (the GPS-confirmation dialog isn't about searching for a place, so the fallback doesn't apply there). Deliberately **not** tied to any search-results state (no "zero results" detection) — it's static guidance shown once per pick session, so it can never interfere with or depend on the actual search/map pipeline.

## Verification

`cd web && npx tsc --noEmit` and `npm run build` clean after every change in this pass. Full `npm test`: 403 passing consistently across every run; failure count fluctuated between runs (44 / 28 / 16 depending on which of `adminAuth.test.mjs`/`discoveryWorkbench.test.mjs` happened to self-skip due to local Docker stack reachability at that moment) but the *pass* count never moved and every failure was confirmed by name to be the same pre-existing `adminAuth`/`discoveryWorkbench`/`workbenchSync` local-state-dependent set — zero regressions from anything in this pass. No physical device/emulator was available to hand-test the actual right-click/long-press context menus; the `contentEditable` mechanism itself is standard, well-documented browser behavior, not something invented for this app, but the real on-device menus were not visually confirmed.

# CI pipeline fixed (2026-09-11 → 2026-09-12) — release deployment still pending explicit approval

A "final production release" pass stalled on a red CI, diagnosed and fixed across three small, separately-authorized commits — each investigated and fixed one at a time, never bundled, per explicit instruction to stop and report rather than improvise past a failed check.

- **`de59c03`** — root `tsconfig.json` was missing an explicit `include`, so TypeScript defaulted to discovering every `.ts`/`.tsx`/`.mjs` file under the repo root as a compilation root. `tests/*.test.mjs` files (picked up this way, via `allowJs`) import `web/src/lib/*.ts` directly, which pulled two web-only files (`extractDimensions.ts`, `supabase.ts` — both use web-specific tsconfig options this project doesn't set) into the root/mobile typecheck. Fixed by scoping `include` to `["App.tsx", "index.ts", "src"]` — config-only, no application logic touched.
- **`011c644`** — with that fixed, CI was *still* red: `node --test "tests/*.test.mjs"` (quoted) passed the glob as a literal string for Node's own test runner to resolve, which worked on local Windows/Node 24 but failed outright on GitHub's Ubuntu runner (`Could not find '.../tests/*.test.mjs'`). Fixed by removing the quotes so the invoking shell expands the glob before Node ever sees it — verified identical behavior in both Git Bash and cmd.exe/PowerShell locally.
- **`63b1500`** — with the glob now resolving, CI hit a *third*, previously-invisible failure: 6 test files (`dishes`/`extractDimensions`/`locationProvenance`/`placeRanking`/`reviews`/`searchCache`) crashed with `ERR_UNKNOWN_FILE_EXTENSION` on `.ts` imports, because `.github/workflows/ci.yml` pinned `node-version: 20` — Node 20 has no built-in TypeScript import support at all, unlike local's Node 24.18.0. Fixed by bumping both CI jobs to `node-version: 24` to match the actual dev/runtime environment. **This was masked the whole time by the first two bugs** — CI never got far enough to hit it until they were fixed.

**CI is now fully green** (`63b1500`, run `34651735904`: both "Root tests + typecheck (mobile)" and "Web typecheck + build" passed). **Nothing has been deployed to Vercel as part of this pass** — per explicit instruction, production deployment is on hold pending separate, explicit approval after CI went green. If resuming this release: the next step is deploying the already-green `main` to the existing production Vercel project, then the full post-deploy verification checklist (homepage/map/search/listing-data/navigation, deployed commit matches `main`), not any further code changes.

# Web map: clustering shipped then reverted same week, default zoom fixed (2026-09-12 → 2026-09-13) — LIVE IN PRODUCTION

**Web-app-only** throughout (explicit instruction each time: do not touch the React Native mobile app for any of this). Two passes, back to back:

## Pass 1 — `306681a`: clustering + viewport-scoped loading

Built to fix a real web performance problem as the real listing count grew (174 in production at the time): `MapView.tsx` rendered one full `AdvancedMarkerElement` per listing with no limit, and `App.tsx` loaded every listing on every page view regardless of what was actually in view. `306681a` added `@googlemaps/markerclusterer` (numbered cluster bubbles, styled to match the app's pink accent, default `onClusterClick`-zooms-in behavior left as-is) plus `web/src/lib/listings.ts`'s `fetchListingsInBounds()` for viewport-scoped fetching, wired through a new debounced `onRegionChange` prop.

**A real bug was found and fixed in the same pass, before it shipped**: viewport-scoped loading could get permanently "stuck" showing a narrow subset even after zooming out to see the whole city, because `mapListings` state (once set by any narrow fetch) never reset. Fixed with a `boundsContain()` check in `App.tsx`'s `handleRegionChange` — falls back to the full listing set whenever the current viewport already covers the whole known city extent. This fix (and the viewport-scoping mechanism itself, `cityBounds`/`padBounds()`/`VIEWPORT_MARGIN_FACTOR`/`hasActiveFilter`/`mapMarkers` in `App.tsx`) was **not** touched by Pass 2 below and remains exactly as shipped.

## Pass 2 — `c22365b`: clustering reverted, default zoom fixed

Direct user feedback after seeing the deployed clustering behavior, in order: numbered cluster bubbles were explicitly rejected ("showing the pins count in map gives us very early with not many restaurant registered" — a low, honestly-displayed listing count reads as an early-stage/sparse platform, which the map should never advertise about itself). A "hybrid" alternative (small local clusters numbered, one city-wide mega-cluster left blank) was proposed, approved, then immediately reversed mid-implementation ("no numbers at all"). A blank/unlabeled decluttering dot was tried next and also rejected by screenshot comparison — the ask was plain individual pins with **zero** decluttering mechanism of any kind, accepting whatever visual overlap naturally occurs at a given zoom, matching a specific reference screenshot the user provided.

**`c22365b` removes `@googlemaps/markerclusterer` entirely** (uninstalled from `web/package.json`, every clustering code path and the `.map-cluster-pin` CSS rule removed from `MapView.tsx`/`styles.css`) — back to exactly the pre-`306681a` one-`AdvancedMarkerElement`-per-listing behavior, with the `306681a` viewport-scoping/bounds-containment fix (a completely separate concern) left untouched.

**A second, distinct bug surfaced once clustering was gone**: the map's default/initial zoom on page load kept drifting wider as the real listing set spread across the metro area, because a one-time `map.fitBounds()` over every loaded listing (`citywideListings` prop, `hasFitInitialBoundsRef`) was overriding the constructor's own fixed `zoom`/`center`. A user-supplied reference screenshot (a moderately-zoomed city-core view showing Jawaharlal Nehru Planetarium, GT World Mall, Cinepolis, ITC Gardenia, Lalbagh Botanical Garden, ASC Golf Course) made the mismatch concrete. Fixed by removing the fit-to-listings effect entirely (and the now-dead `citywideListings` prop/ref, threaded out of `App.tsx` too) and introducing a fixed `DEFAULT_ZOOM` constant. **Tuned by direct visual comparison, not guessed**: `12` was tried first and was too tight (showed IKEA Nagasandra/Jarakabande State Forest, tightly packed); the first "improved" value of `11` was then reported by the user as unchanged ("still the same bkl") — confirmed by a fresh screenshot showing the exact same whole-metro span (Nelamangala Town/Yelahanka/Bannerughatta National Park/Doddaballapura) as the pre-fix behavior. **`13` is the value that actually matches** — verified by taking a live screenshot at `zoom: 13` and confirming every single landmark named in the user's reference screenshot appears in the same relative position. `DEFAULT_CENTER`/`DEFAULT_ZOOM = 13` on the `Map` constructor is now the map's permanent default view; only an explicit search or manual pan/zoom moves the camera from there.

**Lesson worth keeping**: don't trust a screenshot comparison against a *description* of the reference (place names alone) as sufficient verification that a zoom/scale value is right — the `11` value looked reasonable by that same kind of reasoning and was still wrong by two full zoom levels. Actually reproducing the reference's named landmarks in a live screenshot at the candidate zoom, side by side, is what caught it.

**Verified before each commit**: `tsc --noEmit` clean, `npm run build` clean, full suite **397/397 pass** (local Supabase stack stopped for a clean deterministic run, restarted immediately after each time — the dev server depends on it live). **Verified live in production after push** (`c22365b` → `origin/main`, Vercel auto-deploy): fetched the live bundle directly rather than assuming the push deployed — the deployed CSS hash (`main-Byeq0HpK.css`) matched the clean local build byte-for-byte, the deployed JS had zero `MarkerClusterer`/`map-cluster-pin` references, a live Playwright screenshot of `www.beggarsmap.com` showed the same landmark set as the local zoom-13 verification with 165 real pins and 0 cluster bubbles, and homepage/`admin.html` both returned `200` with no new console/page errors (the one pre-existing WebGL-vector-map-fallback warning, already documented elsewhere in this file, is unrelated and harmless).

**Commit scope discipline, worth noting**: at commit time the working tree also held several unrelated uncommitted diffs (the parked React Native mobile diagnostic pass, migration `0025`, `googleMapsLink.ts`'s URL-resolution rewrite, `.claude/settings.json`) — see "Parked / uncommitted" below. Both `306681a` and `c22365b` were staged file-by-file (`web/src/App.tsx`, `web/src/components/MapView.tsx`, `web/src/styles.css`, `web/package.json`, `web/package-lock.json` only) rather than via a broad `git add`, so none of that unrelated parked work was swept into either commit.

# BudgetMap domain migration — Stage 1: budgetmap.in added alongside production — LIVE (2026-09-14)

The web product is migrating brand/domain from **Beggars Map** (`beggarsmap.com`) to **BudgetMap** (`budgetmap.in`), per an approved staged plan (investigation + full sequence in a Claude Code plan-mode session, 2026-09-14 — see that session's saved plan file if it's ever needed again: `claude-baba-we-have-quiet-sparrow.md`). **Stage 1** — adding the new domain alongside the existing live site with zero disruption to it — is complete and browser-verified.

**Update (2026-09-15): the `beggarsmap.com`/`www.beggarsmap.com` → `budgetmap.in` 308 redirect is now confirmed LIVE** — verified directly (`curl -D -` on both legacy hostnames, both return a clean `308 Permanent Redirect` with `Location: https://budgetmap.in/`) during the c4eae0f production-rollout session below. This was configured outside that session (no Vercel/domain action was taken as part of the security rollout) — surfaced as an incidental finding during its final smoke test, not something this repo's own tooling did. Brand copy/canonical-tag/sitemap status beyond what's already noted elsewhere in this file was not re-audited as part of that discovery; don't assume the rest of "Stage 2" is complete just because the redirect is live.

**This stage was 100% external dashboard/DNS configuration — zero repository changes.** No code in this repo gates functionality on a hardcoded domain (Supabase URL/keys are env-var/project-based not domain-based, CSP uses `'self'`, Vite's base path defaults to `/`, admin/discovery OAuth redirects are built from `window.location.origin` at runtime), so nothing needed editing to bring the new domain up:
- **Vercel**: `budgetmap.in` and `www.budgetmap.in` were added as additional domains on the *same* existing production Vercel project (no new project, no separate deployment) — done directly in the Vercel dashboard (this environment has no Vercel credentials).
- **Google Cloud Console**: the browser-side Maps JS API key (`VITE_GOOGLE_MAPS_API_KEY`) had `https://budgetmap.in/*` and `https://www.budgetmap.in/*` added to its HTTP-referrer allowlist, alongside the existing `beggarsmap.com` entries (not removed).
- **Supabase Auth redirect URLs** for `admin.html`/`discovery.html` on the new domain were **not** confirmed added this pass — not required for the public map/search/listings app (anonymous-only, no sign-in), only for the two internal tools; can be added additively whenever needed.

**Verified, from two angles**: (1) HTTP/DNS/TLS-level, via direct `curl`/`WebFetch` from this session — both new hostnames resolve over valid HTTPS and serve **byte-identical HTML** to `www.beggarsmap.com` (same asset hashes: `main-BcVbVpqI.js`/`index-BudeDZFK.js`/`dishes-Bxf3Zxl7.js`/`main-Byeq0HpK.css` — confirms it's the same deployment, not a separate build), and `beggarsmap.com`/`www.beggarsmap.com` were re-confirmed unchanged (same headers, same behavior as before). (2) Interactively, by the user in a real browser on both `budgetmap.in` and `www.budgetmap.in`: Google Maps renders, search works, listing cards render and interact, the app loads normally end to end.

**One nuance surfaced and deliberately left as-is**: the instant both `budgetmap.in` (apex) and `www.budgetmap.in` were attached to the Vercel project, Vercel auto-created a `308` redirect from the apex → `www` (its default behavior when an apex+www pair is added together — the same pattern `beggarsmap.com` itself already uses). This does **not** cross between the two brands/domains (nothing redirects between `budgetmap.in` and `beggarsmap.com`), so it doesn't violate "no redirect between the two domains yet," but it's worth flagging for Stage 2: it's currently backwards relative to the stated eventual-canonical target, which is the **apex** `budgetmap.in` (unlike `beggarsmap.com`, where `www` is canonical) — so this apex→www redirect will likely need flipping (or removing) when Stage 2 makes the apex canonical, not just left as an inherited default.

**Explicitly not touched this stage** (per the approved plan's own constraints): brand copy, canonical `<link>`/OG/Twitter tags (still all `beggarsmap.com`), `robots.txt`/`sitemap.xml` (still all `beggarsmap.com`), analytics (GA4/Cloudflare still shared, no per-domain split), the `beggarsmap.com`→`budgetmap.in` redirect itself (**now live — see the 2026-09-15 update above**), the mobile app, Supabase schema/migrations/APIs/listing IDs/production data.

**This CLAUDE.md entry is the only repository change belonging to Stage 1** — recorded as its own commit, deliberately excluding every unrelated pre-existing uncommitted diff already sitting in the working tree at the time (the parked React Native mobile diagnostic pass, migration `0025` + its test coverage, `googleMapsLink.ts`'s URL-resolution rewrite, `.claude/settings.json`, plus two newer untracked files not yet documented anywhere — `src/lib/mapClustering.ts`/`tests/mapClustering.test.mjs`, mobile-side grid-clustering work) — none of which relate to this migration and none of which this session was asked to resolve. See "Parked / uncommitted" below; that list should be treated as still current except where superseded by a dated section above it.

# Security remediation (c4eae0f) — production rollout COMPLETE (2026-09-15)

Commit `c4eae0f` (`fix: security remediation pass (SSRF, RLS gaps, content moderation, rate limiting, review privacy, SEO canonical, hardening)`) went through a full audit → remediation → local verification → pre-deployment security gate → production rollout sequence, entirely within one Claude Code session on 2026-09-15. **All of it is now deployed and verified live in production.** This section is the closeout record; see that session's own transcript for the full forensic detail (photo/address bug investigation, local fixture cleanup, migration-by-migration production application) if it's ever needed again.

- **Migrations 0026–0031 are applied to production** — photo-URL bucket restriction (0026), reports resolution-field lock (0027), server-side food-relevance content moderation (0028), write rate limiting on `listings`/`listing_reviews` via the new `rate_limit_events` table (0029), the `listing_reviews` privacy boundary (`listing_reviews_public` view + `get_my_review`/`upsert_my_review`/`owns_review` RPCs, 0030), and small hardening (`listing_corrections` length caps + `search_path` on `add_vote`/`remove_vote`, 0031). Applied statement-by-statement via `supabase db query --linked` (the CLI rejects multi-statement input, even via `-f`) rather than `db push` — same established precedent every other post-`0019` migration in this project has used. All 6 defining schema objects, all 4 new triggers (enabled), and the `listing_reviews` grant revocation were independently re-verified live afterward.
- **`discovery-workbench` (v2→v3) and `resolve-maps-link` (v9→v10) are redeployed and ACTIVE** — the SSRF guard (`isPubliclyRoutableUrl`/`fetchImageSafely`, private-IP/DNS-rebinding checks, capped redirect-following) and photo-filename allowlist are live in `discovery-workbench`; the 8s fetch timeout, genericized error message, and `g.co` short-link allowlist addition are live in `resolve-maps-link`. Both confirmed via `functions list` (ACTIVE) and a live smoke call.
- **A production data-only backup was taken immediately before any migration ran**: `supabase/backups/production-pre-c4eae0f-rollout-20260915T001700Z.sql` (1,899,372 bytes). Kept as the rollback point for this specific rollout — gitignored, not committed, same convention as every other backup in `supabase/backups/`.
- **Zero production data was altered by the rollout** — every migration only touched schema (constraints/triggers/functions/views/grants), never rows. `listing_photos` (268), `listing_review_photos` (0), `listing_reviews` (3), and `listing_corrections` (4) counts were confirmed byte-identical before and after.
- **Production listings: 174 visible, 175 total (1 hidden — the pre-existing "yoyo" admin test row)**, confirmed via a privileged (RLS-bypassing) count during rollout verification — this is the first time this project's own tooling ran that privileged count rather than the anon-key/RLS-filtered one, which is why every earlier "174" figure elsewhere in this file (including the "Production listings" note directly below) was always the *visible-only* count and was never wrong; the "175 total" framing is additive information, not a correction. The one previously undercounted row (`"Sri Guru Raghavendra Veg & Dosa Corner"`, `created_at: 2026-09-12T23:46:55`, `is_hidden: false`) predates this rollout by 2+ days and was not created, modified, or touched by anything in it — an ordinary organic submission, same pattern as this project's other unattributed listing-count increases.
- **Local database fixture contamination was found and cleaned up separately, local-only.** A 174-row batch of bare, photo-less, address-less listings (uniform fingerprint: `created_at = 2026-09-13T13:16:21.980581Z`, `source='user'`, `location_source='unknown'`, `location_confidence='unknown'`, `photo_url`/`location_label` both null) had been bulk-inserted into the **local** Docker stack at some point — most likely test/demo data generated for the parked mobile map-clustering work (`src/lib/mapClustering.ts`, dated the same day), since its count (174) exactly matched production's own listing count at the time. This is what caused a real, reproduced local-only symptom (a restaurant's map-detail card showing no photo/no address on `localhost` even though the genuine production row for that same restaurant has both) — diagnosed all the way from DB row → PostgREST query → dev-server-served module → component render logic before concluding it was sparse/duplicate local data, not a frontend or c4eae0f bug. All 174 rows were verified (uniform fingerprint match, zero dependent/FK rows in `listing_photos`/`votes`/`reports`/`listing_corrections`/`listing_reviews`/`reviews`) and deleted from **local only** after a local backup (`npm run db:backup`) and explicit approval. **Local now has 40 genuine listings** (the original Discovery Workbench/Excel-import rows) — 0 duplicates, 0 orphaned dependent rows, the genuine Rajanna/Suhaan rows intact. Production was never touched by this cleanup.
- **Full production smoke test passed**: both domains (`budgetmap.in`/`www.budgetmap.in`) and both legacy-redirect domains (`beggarsmap.com`/`www.beggarsmap.com`, confirmed 308 → `budgetmap.in`) healthy; `admin.html`/`discovery.html` both 200; listings/search reachable; a known photographed/addressed listing (Rajanna) confirmed rendering both fields correctly via the exact anon-key query the frontend uses; the photo URL itself confirmed fetchable; `listing_reviews_public` readable with `created_by` correctly absent, direct `listing_reviews` table reads correctly blocked (`42501`); `admin-listings`/`discovery-workbench` both correctly reject unauthenticated calls (401); `resolve-maps-link` responds live with the new `g.co` allowlist confirmed active. **No browser-automation tool was available in either the rollout or closeout session** — every check above is HTTP/API-level (`curl`, direct PostgREST/Edge-Function calls), not a rendered-DOM/visual/click-through verification; that gap is explicit, not glossed over.

**Two known follow-ups from the rollout — one now RESOLVED, one still open:**
1. ~~**`0028`'s content-moderation trigger does not actually skip a raw, JWT-less Postgres connection.**~~ — **FIXED, commit `bb4d66b`, applied to production 2026-09-15 (same day, before Batch 9 below was pushed).** Root cause: the trigger read `if auth.role() = 'service_role' then return new; end if;` — for a connection with no PostgREST JWT context (e.g. `supabase db query --linked`, which is how `tools/discovery/import-excel.mjs --production --execute` writes), `auth.role()` returns `NULL`, and `NULL = 'service_role'` evaluates to `NULL`, which Postgres's `IF` treats as false — so the check was **not** bypassed, contrary to 0028's own comment claiming the importer "is unaffected." New migration `0032_fix_content_moderation_service_role_guard.sql` (0028 itself was **not** edited) replaces `check_listing_food_relevance()` via `CREATE OR REPLACE FUNCTION`, restructuring the guard to the same `auth.role() <> 'service_role'`-wraps-the-check shape every other lock/check trigger in this schema already uses — identical keyword list/regex/exception, only the guard polarity changed. Verified both locally and directly against production: an anon/authenticated PostgREST insert with a non-food name is still rejected (`P0001`); a raw Postgres connection insert with a moderation-keyword-bearing note now succeeds instead of being incorrectly blocked (production verification row inserted then immediately deleted, zero residue). All 6 pre-existing 0026–0031 schema objects and every other function's source were re-confirmed unchanged.
2. **`discovery-workbench`'s `isPubliclyRoutableUrl()` has no explicit timeout on its `Deno.resolveDns()` calls** — only the later `fetch()` call is timeout-wrapped (`AbortSignal.timeout(10000)`). A hung DNS lookup could stall indefinitely, bounded only by Supabase's platform-level outer timeout. Assessed LOW-MEDIUM severity in the original security gate; still not fixed, still the one open item.

# Current Project State (as of 2026-09-12)

Concise, factual snapshot of what is actually true right now — kept separate from the roadmap above, which is durable/forward-looking. Full narrative and historical detail for everything below lives in AGENTS.md; this section exists so a fresh session can get oriented without reading that much longer log first. Verify against AGENTS.md and production directly before relying on this for anything consequential — it decays the same way any status snapshot does.

## Production listings

**422 listings live in production, 2 hidden** ("N Mart tea stall", a pre-existing user-submitted listing, plus one deliberately held-back Discovery Workbench import — the Vijayanagar "Rajanna Military Hotel", a probable duplicate awaiting a human decision; see Batch 16's closeout), confirmed directly via a fresh read-only query on 2026-09-24, after Discovery Workbench Batch 16's 29-listing import (28 unhidden). Production was re-checked at exactly 393 immediately before Batch 16's import began, matching Batch 15's closing count with no unattributed drift. Re-verify before treating any of these numbers as current.

## WIP xlsx: "Number Valid = No Answer" count (as of 2026-09-18)

**178 of the 3,586 total rows** in the local WIP xlsx (`tools/discovery/output/candidates-2026-09-01T11-50-51-056Z workonprogress.xlsx`) have `Number Valid = "No Answer"`, counted directly via `xlsx-to-json.py` against the real file (not the parked, never-shipped "No Answer counter" feature — see "Parked / uncommitted" below). This is a simple point-in-time snapshot, not a tracked/live metric anywhere in this repo; re-count directly from the xlsx if this number matters again later, since ongoing batch review keeps changing it.

## Discovery Workbench Batch 3 — completed and published

Fully closed out, end to end:
- 100 candidates were researched by a remote intern through the deployed `discovery.html` page against **production**'s own `discovery-workbench` Edge Function — a separate path from the owner's local `workbench-sync.mjs` (see below).
- All 100 rows' completed research (Number Valid / Menu List Under 100 / Menu Details/Notes) were pulled from production into the local WIP xlsx and verified field-for-field; photos were downloaded to `tools/discovery/photos/<place_id>/`.
- Production's `discovery_batch_rows` was purged clean afterward (0 rows), verified directly.
- Of the 100, 42 qualified (`Menu List Under 100 = Yes`): 32 were new inserts (their prices corrected by `import-excel.mjs`'s rupee-prefix fix, commit `2cdab35`, after 11 were initially found using a bare quantity number instead of the real price); 10 were already-imported duplicates, correctly skipped.
- All 32 new listings were imported `is_hidden = true`, then unhidden via the new admin `bulkUnhide` action (see commits below) — confirmed live, 0 hidden remain.

## Discovery Workbench Batch 4 — completed and published

Fully closed out end to end, following the same lifecycle as Batch 3 (see "The established Discovery Workbench batch lifecycle" below):
- 100 candidates were pushed locally (`workbench-sync.mjs --push --batch-size=100`), then transferred to production's `discovery_batch_rows` for the intern via the same one-time production-transfer step Batch 3 needed.
- Of the 100, 94 had at least one researched field filled in; 29 qualified (`Menu List Under 100 = Yes`), 33 were marked `No`, 6 were left fully blank by the intern.
- All 100 rows' research was pulled from production into the local WIP xlsx and verified field-for-field (94 rows updated, 0 unrelated rows touched, 0 conflicts with existing Excel data); 45 photos across 29 place_ids were downloaded to `tools/discovery/photos/<place_id>/`.
- Production's `discovery_batch_rows` and `discovery-photos` bucket were purged clean afterward (0 rows, 0 files), verified directly.
- All 29 qualifying rows were new inserts (0 already-imported duplicates from this batch) — imported `is_hidden = true` via `import-excel.mjs --production --execute`, then unhidden via the admin `bulkUnhide` action — confirmed live: production reached 89 listings, 0 hidden.

## Discovery Workbench Batch 5 — closed out 2026-09-11 (was left partially researched)

Originally pushed 2026-09-05 (100 candidates, 0 photos) and staged in production the same way Batches 3/4 were. Sat there for several days; when actually checked again on 2026-09-11 (before assuming it was "done" and safe to replace), it was only **51/100 reviewed** — 49 rows still had `Menu List Under 100` blank. Do not assume a staged batch is finished just because time has passed; check `discovery_batch_rows` directly.

Before closing it out, all 51 completed rows (including the 16 that had dish/price notes) were cross-checked field-by-field against the local WIP xlsx: **zero mismatches, zero missing** — someone/something had already pulled this batch's progress into Excel correctly at some point, so nothing was at risk of being lost by purging. Purged via the standard direct method below (production `discovery_batch_rows`: 100→0 rows; `discovery-photos` storage: 30→0 objects across 15 place_ids, all 15 confirmed already backed up in `tools/discovery/photos/<place_id>/` before deletion). Local's own separate copy (which, oddly, mirrored production's exact 51/100 progress byte-for-byte — never fully explained, most likely a deliberate earlier test mirror, not investigated further since it didn't change what needed doing) was closed out the normal way: `--pull` then `--pull --purge`.

**Incident during this purge, caught immediately and fixed**: `supabase storage rm ss:///discovery-photos --linked --experimental --recursive --yes` didn't just empty the bucket — it deleted the `discovery-photos` bucket itself from `storage.buckets` (`storage ls ss:///` afterward showed only `listing-photos`). Recovered in the same session by re-running migration `0021`'s own bucket-creation `INSERT` verbatim (`id/name='discovery-photos', public=false, allowed_mime_types=['image/jpeg','image/png','image/webp'], file_size_limit=2097152`, `ON CONFLICT DO NOTHING`) and confirming the restored config matched exactly. No data was actually lost (photos were already backed up locally first), but this is now a documented, real gap in the Batch 3/4 "safe purge" precedent — see the safeguards list below for the standing rule this created.

## Discovery Workbench Batch 6 — pushed at the wrong size, immediately corrected, no longer exists

Directly after closing out Batch 5, a fresh batch was pushed using the CLI's own raw default (`--push` with no `--batch-size`, i.e. 50) instead of the project's actual established practice. Checking batch history properly (`workbench-state.json`'s own `completed` records) showed batches 2/3/4/5 were **all exactly 100** — 50 was never the real convention, just the tool's own unrelated hardcoded default. Since batch 6 was completely fresh (0/50 reviewed, in both local and production, pushed only minutes earlier), it was purged again immediately (local `--pull --purge`; production `discovery_batch_rows` DELETE) with zero data at risk, and replaced by Batch 7 below. **If pushing a batch in the future, use `--batch-size=100` explicitly — don't rely on the CLI's own default.**

## Discovery Workbench Batch 7 — closed out 2026-09-12 (was left partially researched)

Pushed locally with `workbench-sync.mjs --push --batch-size=100` (100 candidates, 0 photos) and transferred to production the same way Batches 3/4/5 were. When actually checked again on 2026-09-12 (a session that initially assumed it was "batch 6" — it wasn't; batch 6 never got past a same-day correction, see above), it was only **87/100 reviewed** — 13 rows still had `Menu List Under 100` blank. Same lesson as Batch 5: checked real progress directly before touching anything, rather than trusting the doc snapshot or the batch number a passing mention used.

Closed out anyway, by explicit choice, rather than waiting for the remaining 13: nothing was at risk, since a blank cell purged is just a blank cell purged.

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via the same backup-then-write-then-verify sequence `workbench-sync.mjs`'s own `--pull` uses internally (a one-off script replicated that exact logic against production data, since the real tool has no `--linked` path) — verified field-for-field, all 100 rows' `phone`/`Number Valid`/`Menu List Under 100`/`Menu Details/Notes` matched production exactly after the write.
- **Photos**: 52 photos across 28 place_ids downloaded from production's `discovery-photos` bucket via `supabase storage cp -r` (developer-login auth, no service-role key involved), verified byte-for-byte present on disk before any deletion.
- **Purge**: production `discovery-photos` — 52 objects deleted by exact path (never a bucket-root recursive delete — the Batch 5 incident's standing rule), bucket itself confirmed still present afterward (`buckets_deleted: []`). Production `discovery_batch_rows`: 100 → 0 rows. Local's own separate batch-7 copy (100 rows, all still blank — the intern only ever worked against production) was deleted directly from the local Docker stack rather than via `--pull` (pulling would have overwritten the just-verified real Excel data with local's blanks — the same class of risk the Batch 5 stale-local-rows note warns about), and `workbench-state.json` was hand-reconciled to move all 100 place_ids from `in_progress` to `completed` to match. `workbench-sync.mjs --status` afterward confirmed 0 in-progress, ready for a future `--push`.
- **Import**: of the 113 rows now marked `Menu List Under 100 = Yes` in Excel, 86 were already-imported duplicates (correctly skipped, 0 false positives), **27 were new** — dry-run reviewed and approved, then `import-excel.mjs --production --execute` inserted all 27 (`is_hidden = true`) plus 50 `listing_photos` rows/uploads, zero UPDATEs/DELETEs. Production went from 114 → 141 listings.
- **Unhide**: all 27 new listing IDs flipped `is_hidden = false` via a direct `UPDATE ... WHERE id IN (...)` through `db query --linked` — this works because that connection carries no PostgREST JWT context, so `auth.role()` reads `NULL` and the `lock_listing_admin_fields()` trigger's `auth.role() <> 'service_role'` guard evaluates to `NULL`/false and skips (no service-role key ever read or used). Verified two ways: an independent re-`SELECT` confirmed all 27 `is_hidden = false` and production's overall hidden count returned to exactly 1 (the pre-existing "yoyo" test row); a real anon-key REST read (`GET .../rest/v1/listings?select=id,name,price_rupees`) confirmed one of the 27 ("Punjab-e-Dhaba") is genuinely publicly readable, not just true in the privileged view.
- **The 13 rows that stayed unreviewed are now permanently marked `completed` in the state file**, same as Batch 5's own leftover unreviewed rows — `workbench-sync.mjs`'s push-eligibility check excludes any `completed` place_id regardless of whether its Excel cell is still blank, so these 13 will **not** automatically resurface in a future `--push`. If they should be re-offered to a future intern, that needs a deliberate, explicit state-file edit (remove them from `completed`) — not done here, since it wasn't asked for.
- Final verified state: production **141 listings, 1 hidden** (unchanged pre-existing test row); local Discovery Workbench fully reconciled (0 in-progress, 553 completed, 3017 remaining eligible for a future batch).

## Discovery Workbench Batch 8 — closed out 2026-09-12 (was left partially researched, same day it was pushed)

Pushed locally and transferred to production 2026-09-12 (see the push record this section used to hold, now superseded). Checked again the same day before touching anything (by-now standing practice, not optional) and found only **76/100 reviewed** — 24 rows still had `Menu List Under 100` blank. Closed out anyway, same explicit choice as Batches 5 and 7: a blank cell purged loses nothing.

- **Pull**: all 100 production rows read directly and written into the WIP xlsx via the same backup-then-write-then-verify one-off script used for Batch 7 — verified field-for-field, all 100 rows' `phone`/`Number Valid`/`Menu List Under 100`/`Menu Details/Notes` matched production exactly.
- **Photos**: 56 photos across 31 place_ids downloaded from production's `discovery-photos` bucket, verified byte-for-byte on disk. **Worth noting for next time**: the download loop (31 sequential `supabase storage cp -r` calls) took long enough that an early "is it still running" check via `tasklist`/`ps` caught a brief gap between successive CLI invocations and produced a false "finished" reading, part-way through with 20 of 31 place_ids still missing. Caught before anything downstream used the data, by cross-checking the download log's actual file size for growth over repeated checks rather than trusting a single process-list snapshot — the same class of check that should be used for any other long-running loop of sequential CLI calls, not just this one.
- **Purge**: production `discovery-photos` — 56 objects deleted by exact path, bucket confirmed still present (`buckets_deleted: []`). Production `discovery_batch_rows`: 100 → 0. Local's separate stale copy (100 rows, still blank, 0 local photos) deleted directly from the local Docker stack; `workbench-state.json` hand-reconciled to move all 100 place_ids from `in_progress` to `completed`. `workbench-sync.mjs --status` confirmed 0 in-progress afterward.
- **Import**: of 146 rows now `Menu List Under 100 = Yes`, 113 were already-imported duplicates (correctly skipped), **33 were new** — dry-run reviewed and approved, then executed: 33 listings + 56 `listing_photos` rows/uploads, `is_hidden = true`, zero UPDATEs/DELETEs. Production went from 141 → 174 listings.
- **Unhide**: all 33 flipped `is_hidden = false` via direct SQL (same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as Batch 7, no service-role key used). Verified independently: re-`SELECT` confirmed all 33 `is_hidden = false` and production's hidden count returned to exactly 1; a real anon-key REST read confirmed one of the 33 ("Kolkata king") is genuinely publicly readable.
- **The 24 unreviewed rows are now permanently `completed`** in the state file, same consequence as every prior partial-batch closeout — they won't resurface in a future `--push` without a deliberate state-file edit.
- Final verified state: production **174 listings, 1 hidden**; local Discovery Workbench fully reconciled (0 in-progress, 653 completed, 2917 remaining eligible for a future batch).

## Discovery Workbench Batch 9 — closed out 2026-09-16 (was left partially researched)

Pushed 2026-09-15 the same day as (and after) the `0028` importer-safety fix above (0032/`bb4d66b`) — deliberately sequenced that way, since this batch's eventual import goes through the raw-connection path that fix targets. Production transfer used the same one-off manual `supabase db query --linked` INSERT this step has always used, built via a small throwaway Node script, production's exact column list (local's extra `no_answer_count` column correctly excluded), and the standard `sqlString`/`chr(N)` escaping.

Checked again the next day (2026-09-16) before touching anything — standing practice — and found only **71/100 reviewed**: 29 rows `Menu List Under 100 = Yes`, 42 `= No`, 29 still blank. Closed out anyway, same explicit choice as every prior partial batch (5/7/8): a blank cell purged loses nothing.

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via a one-off script replicating `workbench-sync.mjs`'s own `--pull` logic (production's `notes` column is already server-derived from `dishes` by the Edge Function's `update` action, so no separate dish-to-sentence conversion was needed) — verified field-for-field, all 100 rows' `phone`/`Number Valid`/`Menu List Under 100`/`Menu Details/Notes` matched production exactly after the write.
- **Photos**: 55 photos across 29 place_ids downloaded from production's `discovery-photos` bucket via `supabase storage cp -r`, verified byte-for-byte present on disk (a path-nesting mistake during download — files initially landed at `photos/<place_id>/<place_id>/<file>` instead of `photos/<place_id>/<file>` — was caught and corrected before the import step touched them).
- **Purge**: production `discovery-photos` — 55 objects deleted by **exact path list** (`supabase storage rm ss:///discovery-photos/<place_id>/<file> ... --yes`, never a bucket-root recursive delete — the Batch 5 incident's standing rule), bucket itself confirmed still present afterward. Production `discovery_batch_rows`: 100 → 0 rows. Local's own separate stale batch-9 copy (100 rows, all still blank, 0 local photos) deleted directly from the local Docker stack; `workbench-state.json` hand-reconciled to move all 100 place_ids from `in_progress` to `completed`. `workbench-sync.mjs --status` afterward confirmed 0 in-progress.
- **Import**: of the rows now `Menu List Under 100 = Yes` (175 total in Excel), 146 were already-imported duplicates (correctly skipped, 0 false positives — including one legitimate same-name-different-branch case, "Shrinidhi Military Hotel," ~6.4km from the existing JP Nagar listing), **29 were new** — dry-run reviewed and approved, then `import-excel.mjs --production --execute` inserted all 29 (`is_hidden = true`) plus 55 `listing_photos` rows/uploads, zero UPDATEs/DELETEs. Production went from 176 → 205 listings.
- **Unhide**: all 29 new listing IDs flipped `is_hidden = false` via direct `UPDATE ... WHERE id IN (...)` through `db query --linked` (same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as every prior batch, no service-role key used). Verified independently: re-`SELECT` confirmed all 29 `is_hidden = false` and production's overall hidden count returned to exactly 1 (the pre-existing "yoyo" test row); real anon-key REST reads (`GET .../rest/v1/listings?select=id,name,price_rupees`) confirmed several of the 29 (Rajanna Military, Street La La La, Sri Sai Darshini Restaurant, Aaradhana veg) are genuinely publicly readable, not just true in the privileged view.
- **The 29 rows that stayed unreviewed are now permanently marked `completed`** in the state file, same consequence as every prior partial-batch closeout — they won't resurface in a future `--push` without a deliberate state-file edit.
- Final verified state: production **205 listings, 1 hidden** (unchanged pre-existing test row); local Discovery Workbench fully reconciled (0 in-progress, 753 completed, 2817 remaining eligible for a future batch).

## Discovery Workbench Batch 10 — closed out 2026-09-16 (was left partially researched)

Pushed 2026-09-16 locally (`workbench-sync.mjs --push --batch-size=100`) and transferred to production the same one-off manual `supabase db query --linked` INSERT method every batch has used. This time the request explicitly gated each stage behind a separate approval (push-only, then verify; transfer-only, then verify) before the closeout below — each stage was independently verified against both local and production (exact row/place_id counts, zero overlap with the 753 prior-`completed` place_ids, existing listings/photos bucket byte-unchanged) before the next was allowed to proceed.

Checked again same-day before touching anything — standing practice — and found only **84/100 reviewed**: 29 rows `Menu List Under 100 = Yes`, 55 `= No`, 16 still blank. Closed out anyway, same explicit choice as every prior partial batch: a blank cell purged loses nothing.

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via the same one-off pull-replication script used for Batch 9 — verified field-for-field, all 100 rows' `phone`/`Number Valid`/`Menu List Under 100`/`Menu Details/Notes` matched production exactly after the write.
- **Photos**: 53 photos across 28 place_ids downloaded from production's `discovery-photos` bucket via `supabase storage cp -r` into the photos dir directly (the Batch 9 path-nesting mistake was avoided this time by pointing the copy at the parent `photos/` dir rather than a pre-made `photos/<place_id>/` dir), verified byte-for-byte present on disk with a correctly flat `photos/<place_id>/<file>` structure before any deletion.
- **Purge**: production `discovery-photos` — 53 objects deleted by **exact path list**, bucket itself confirmed still present afterward. Production `discovery_batch_rows`: 100 → 0 rows. Local's own separate stale batch-10 copy (100 rows, all still blank, 0 local photos) deleted directly from the local Docker stack; `workbench-state.json` hand-reconciled to move all 100 place_ids from `in_progress` to `completed`. `workbench-sync.mjs --status` afterward confirmed 0 in-progress.
- **Import**: of the rows now `Menu List Under 100 = Yes` (204 total in Excel), 175 were already-imported duplicates (correctly skipped, 0 false positives), **29 were new** — dry-run reviewed and approved (including a same-batch naming check: two new rows both named "Namaste," confirmed as genuinely different restaurants ~17km apart in Kalyan Nagar vs. Vijaya Nagar, different place_ids/photos/menus, not a duplicate), then `import-excel.mjs --production --execute` inserted all 29 (`is_hidden = true`) plus 53 `listing_photos` rows/uploads, zero UPDATEs/DELETEs. Production went from 205 → 234 listings.
- **Unhide**: all 29 new listing IDs flipped `is_hidden = false` via direct `UPDATE ... WHERE id IN (...)` through `db query --linked` (same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as every prior batch, no service-role key used). Verified independently: re-`SELECT` confirmed all 29 `is_hidden = false` and production's overall hidden count returned to exactly 1 (the pre-existing "yoyo" test row); a real anon-key REST read (`GET .../rest/v1/listings?select=id,name,price_rupees`) confirmed several of the 29 (Eat Bengaluru Restaurant, Sai Sogadu, Maruthi military hotel) are genuinely publicly readable, not just true in the privileged view.
- **The 16 rows that stayed unreviewed are now permanently marked `completed`** in the state file, same consequence as every prior partial-batch closeout — they won't resurface in a future `--push` without a deliberate state-file edit.
- Final verified state: production **234 listings, 1 hidden** (unchanged pre-existing test row); local Discovery Workbench fully reconciled (0 in-progress, 853 completed, 2717 remaining eligible for a future batch).

## Discovery Workbench Batch 11 — closed out 2026-09-18 (was left partially researched)

Pushed and transferred to production 2026-09-17 (see the push/transfer record this section used to hold, now superseded by the closeout below). Checked again the next day before touching anything — standing practice — and found only **79/100 reviewed**: 23 rows `Menu List Under 100 = Yes`, 56 `= No`, 21 still blank. Closed out anyway, same explicit choice as every prior partial batch: a blank cell purged loses nothing.

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via the same one-off pull-replication script used for Batches 9/10 — verified field-for-field, all 100 rows' `phone`/`Number Valid`/`Menu List Under 100`/`Menu Details/Notes` matched production exactly after the write.
- **Photos**: 44 photos across 23 place_ids downloaded from production's `discovery-photos` bucket via `supabase storage cp -r` into the parent photos dir directly (same correctly-flat method as Batch 10), verified byte-for-byte present on disk with a correctly flat `photos/<place_id>/<file>` structure before any deletion.
- **Purge**: production `discovery-photos` — 44 objects deleted by **exact path list**, bucket itself confirmed still present afterward. Production `discovery_batch_rows`: 100 → 0 rows. Local's own separate stale batch-11 copy (100 rows, all still blank, 0 local photos) deleted directly from the local Docker stack; `workbench-state.json` hand-reconciled to move all 100 place_ids from `in_progress` to `completed`. `workbench-sync.mjs --status` afterward confirmed 0 in-progress.
- **Import**: of the rows now `Menu List Under 100 = Yes` (227 total in Excel), 204 were already-imported duplicates (correctly skipped, 0 false positives), **23 were new** — dry-run reviewed and approved (0 same-batch naming collisions this time, unlike Batch 10's "Namaste" case), then `import-excel.mjs --production --execute` inserted all 23 (`is_hidden = true`) plus 44 `listing_photos` rows/uploads, zero UPDATEs/DELETEs. Production went from 234 → 257 listings.
- **Unhide**: all 23 new listing IDs flipped `is_hidden = false` via direct `UPDATE ... WHERE id IN (...)` through `db query --linked` (same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as every prior batch, no service-role key used). Verified independently: re-`SELECT` confirmed all 23 `is_hidden = false` and production's overall hidden count returned to exactly 1 (the pre-existing "yoyo" test row); a real anon-key REST read (`GET .../rest/v1/listings?select=id,name,price_rupees`) confirmed several of the 23 (Andhra Food Company, Naivedhya Tiffin's, Sai Jaganath Food And Chats Point) are genuinely publicly readable, not just true in the privileged view.
- **The 21 rows that stayed unreviewed are now permanently marked `completed`** in the state file, same consequence as every prior partial-batch closeout — they won't resurface in a future `--push` without a deliberate state-file edit.
- Final verified state: production **257 listings, 1 hidden** (unchanged pre-existing test row); local Discovery Workbench fully reconciled (0 in-progress, 953 completed, 2617 remaining eligible before Batch 12 below was pushed).

## Discovery Workbench Batch 12 — closed out 2026-09-18 (was left partially researched)

Pushed and transferred to production 2026-09-18 (see the push/transfer record this section used to hold, now superseded by the closeout below). Checked again the same day before touching anything — standing practice — and found only **71/100 reviewed**: 42 rows `Menu List Under 100 = Yes`, 29 `= No`, 29 still blank. Closed out anyway, same explicit choice as every prior partial batch: a blank cell purged loses nothing.

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via the same one-off pull-replication script used for Batches 9-11 — verified field-for-field, all 100 rows' `phone`/`Number Valid`/`Menu List Under 100`/`Menu Details/Notes` matched production exactly after the write (400/400 field values, 0 mismatches).
- **Photos**: 79 photos across 42 place_ids downloaded from production's `discovery-photos` bucket via `supabase storage cp -r`. This run landed nested one level too deep (`photos/discovery-photos/<place_id>/<file>`, the exact Batch 9 mistake) — caught immediately and corrected by flattening to `photos/<place_id>/<file>` before any deletion, then re-verified byte-for-byte (42 place_ids, 79 files matching production's object list exactly).
- **Purge**: production `discovery-photos` — 79 objects deleted by **exact path list**, bucket itself confirmed still present afterward (`buckets_deleted: []`, and a re-list showed the bucket empty). Production `discovery_batch_rows`: 100 → 0 rows. Local's own separate stale batch-12 copy (100 rows, all still blank, byte-identical place_id set to production's, 0 local photos) deleted directly from the local Docker stack (Docker Desktop was mid-startup at the start of this closeout — confirmed running before this step); `workbench-state.json` hand-reconciled to move all 100 place_ids from `in_progress` to `completed`. `workbench-sync.mjs --status` afterward confirmed 0 in-progress.
- **Import**: of the rows now `Menu List Under 100 = Yes` (269 total in Excel), 227 were already-imported duplicates (correctly skipped, 0 false positives) and 1 was correctly flagged and excluded as duplicate-risk (`Udupi Vaibhava, Veg Restaurant and Party Hall`, 43% name overlap with an existing listing 3m away — left for a human decision, not imported), **41 were new** — dry-run reviewed and approved (0 same-batch naming collisions; one look-alike pair, "Dakshin Thindi" vs "Dakshin Thindi@Jayanagar 5th block", confirmed as genuinely different branches ~2.2km apart with distinct addresses), then `import-excel.mjs --production --execute` inserted all 41 (`is_hidden = true`) plus 78 `listing_photos` rows/uploads, zero UPDATEs/DELETEs. Production went from 257 → 298 listings.
- **Unhide**: all 41 new listing IDs flipped `is_hidden = false` via direct `UPDATE ... WHERE id IN (...)` through `db query --linked` (same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as every prior batch, no service-role key used). Verified independently: re-`SELECT` confirmed all 41 `is_hidden = false` and production's overall hidden count returned to exactly 1 (the pre-existing "yoyo" test row); a real anon-key REST read (`GET .../rest/v1/listings?select=id,name,price_rupees`) confirmed several of the 41 (Coffee Times, SRI UDUPI VEG, Devi upahar) are genuinely publicly readable, not just true in the privileged view.
- **The 29 rows that stayed unreviewed are now permanently marked `completed`** in the state file, same consequence as every prior partial-batch closeout — they won't resurface in a future `--push` without a deliberate state-file edit.
- Final verified state: production **298 listings, 1 hidden** (unchanged pre-existing test row); local Discovery Workbench fully reconciled (0 in-progress, 1053 completed, 2517 remaining eligible for a future batch).

## Discovery Workbench Batch 13 — closed out 2026-09-21 (was left partially researched)

Pushed and transferred to production 2026-09-18 (see the push/transfer record this section used to hold, now superseded by the closeout below). Checked again 2026-09-20 before touching anything — standing practice — and found only **67/100 reviewed**: 34 rows `Menu List Under 100 = Yes`, 33 `= No`, 33 still blank. Closed out anyway, same explicit choice as every prior partial batch: a blank cell purged loses nothing. Production was re-checked immediately before the purge (a day after the pull) and was byte-identical to what had been pulled — the intern had added nothing in between.

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via a one-off script replicating `workbench-sync.mjs`'s own `--pull` logic (pre-checked that state's `in_progress` matched production's place_ids exactly and that no target row already held a `Menu List Under 100` value; backed up to `output/backups/PRE-BATCH13-PULL-...bak.xlsx` first) — verified field-for-field, 400/400 values matched, 0 unrelated rows changed.
- **Photos**: 64 photos across 34 place_ids downloaded **one object at a time to its exact flat path** (`supabase storage cp ss:///discovery-photos/<pid>/<file> tools/discovery/photos/<pid>/<file>`), which avoids the nested-directory mistake Batches 9 and 12 hit; all 64 verified byte-size-for-byte against production's `storage.objects` metadata before any deletion. The download loop is long (~64 sequential CLI calls) — a backgrounded shell command's own "completed" notification only means the wrapper exited, not the loop; wait for an explicit DONE marker.
- **Purge**: production `discovery-photos` — 64 objects deleted by **exact path list**, bucket confirmed still present. Production `discovery_batch_rows`: 100 → 0. Local's stale batch-13 copy (100 rows, all blank) verified against production's place_id set, then deleted (`delete ... where batch_id='13'` must be its own single statement — the CLI rejects a trailing `; select`); `workbench-state.json` hand-reconciled (all 100 `in_progress` → `completed`, 1153 completed).
- **Import**: of 303 rows now `Menu List Under 100 = Yes`, 268 were already-imported duplicates (correctly skipped), 1 was correctly flagged and excluded as duplicate-risk (the same `Udupi Vaibhava, Veg Restaurant and Party Hall` row Batch 12 already left for a human decision), **34 were new** — dry-run reviewed and approved (0 same-batch naming collisions), then `import-excel.mjs --production --execute` inserted all 34 (`is_hidden = true`) plus 64 `listing_photos` rows/uploads, zero UPDATEs/DELETEs. Production went from 298 → 332 listings and 498 → 562 photo rows. The execute run takes >10 min (64 uploads + 34 inserts) and outlives the shell tool's own timeout — it keeps running in the background and is resumable.
- **Unhide**: all 34 flipped `is_hidden = false` via direct `UPDATE ... WHERE source='import' AND is_hidden AND id IN (...)` through `db query --linked`, IDs taken from the DB's recent hidden imports and cross-checked against the importer's own production state-file entries (all 34 matched) — same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as every prior batch, no service-role key used. Verified: an anon-key REST read confirmed several of the 34 (Arogya Bhojan, Shree Thali, Dakshin Delicacy, New Peacock Restaurant, Udupi Vaibhav) publicly readable, and the anon-visible total is 331 (332 minus 1 hidden).
- **A second "Udupi Vaibhav" now exists** (₹50 and ₹55) — both passed the importer's duplicate check, so presumably different branches; worth a human glance, not acted on.
- **Garbled names are a source-data flaw, not a transfer bug.** 3 of this batch's 100 (`Iris CafÃ© & Kitchen`, `Kapoorâ€™s Cafe Jayanagar`, `Shawarmaniac â„¢ @ Begur Road`) were already mojibake in the xlsx's own `name` column (from the original discovery data) — the push/transfer path (`sqlString`'s `chr(N)` escaping) is correct and preserves whatever the xlsx holds. None of the 3 qualified (`Menu List Under 100 = No`), so no live listing carries a garbled name (verified: the only 4 live non-ASCII names are legitimate Kannada/curly-apostrophe ones). Worth knowing if a future batch's intern reports oddly-spelled names.
- **The 33 rows that stayed unreviewed are now permanently marked `completed`** in the state file, same consequence as every prior partial-batch closeout.
- **Correction to older notes**: the one hidden production listing is actually **"N Mart tea stall"** (`source='user'`, last updated 2026-09-10), not "yoyo" — "yoyo" is visible. Earlier batch sections' "hidden count returned to exactly 1 (the pre-existing yoyo test row)" was true as a count but misnamed the row. Neither was touched by this closeout.
- Final verified state: production **332 listings, 1 hidden**; local Discovery Workbench fully reconciled before Batch 14 below was pushed.

## Discovery Workbench Batch 14 — closed out 2026-09-22 (was left partially researched)

Pushed and transferred to production 2026-09-21 (see the push/transfer record this section used to hold, now superseded by the closeout below). Checked again 2026-09-22 before touching anything — standing practice — and found only **69/100 reviewed**: 28 rows `Menu List Under 100 = Yes`, 41 `= No`, 31 still blank. Closed out anyway, same explicit choice as every prior partial batch: a blank cell purged loses nothing. This closeout, and the Batch 15 push+transfer below, were run directly (not through a plan-mode audit session) at the owner's explicit request for "another batch of restaurant data," reusing the exact established lifecycle documented in this section without re-deriving it.

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via `write-updates-to-xlsx.py` (the existing script — its own header documents the lock-file/atomic-replace/missing-column safety properties), fed a `{place_id: {column: value}}` payload built from the pulled rows; pre-checked that local's `in_progress` set matched production's 100 place_ids exactly and that none of the 100 xlsx rows already held a `Menu List Under 100` value; backed up to `output/backups/PRE-BATCH14-PULL-...bak.xlsx` first — verified field-for-field afterward, 400/400 values matched, 0 mismatches.
- **Photos**: 54 photos across 28 place_ids downloaded **one object at a time to its exact flat path**, matching Batch 13's method (avoids the nested-directory mistake Batches 9 and 12 hit); all 54 verified byte-size-for-byte against production's `storage.objects` metadata before any deletion. The download loop (~54 sequential CLI calls) ran much slower than prior batches this time (network-latency-bound, ~7-8s/object) — confirmed genuinely still running via file-timestamp growth rather than trusting a single snapshot, consistent with the standing lesson from Batch 8's own note on this.
- **Purge**: production `discovery-photos` — 54 objects deleted by **exact path list**, bucket confirmed still present (`buckets_deleted: []`, re-list showed empty). Production `discovery_batch_rows`: 100 → 0. Local's stale batch-14 copy (100 rows, all still blank, place_id set confirmed byte-identical to production's before deleting) removed directly from the local Docker stack; `workbench-state.json` hand-reconciled (all 100 `in_progress` → `completed`, 1253 completed). `workbench-sync.mjs --status` afterward confirmed 0 in-progress.
- **Import**: of 331 rows now `Menu List Under 100 = Yes`, 302 were already-imported duplicates (correctly skipped, 0 false positives), 1 was correctly flagged and excluded as duplicate-risk (the same `Udupi Vaibhava, Veg Restaurant and Party Hall` row left for a human decision since Batch 12), **28 were new** — dry-run reviewed and approved (0 same-batch naming collisions; two same-name-different-branch checks against already-imported listings — a new "Thalassery Restaurant" ~16-18km from the existing one, and a new "Sri Udupi Park" ~11-12km from the existing one, both confirmed genuinely different branches by comparing coordinates directly), then `import-excel.mjs --production --execute` inserted all 28 (`is_hidden = true`) plus 54 `listing_photos` rows/uploads, zero UPDATEs/DELETEs. **This execute run outlived the shell tool's own timeout partway through** (6 of 28 landed on the first foreground call before it was cut off) — resumed in the background per the importer's own resumable/idempotent design (state file + live duplicate-check skip already-done rows); network latency made each remaining item take several minutes, so this ran as a long-lived background task checked periodically against ground-truth production listing counts (not just the log file, which buffers when piped to a file) until all 28 landed. Production went from 332 → 360 listings and 562 → 616 photo rows. One imported photo (`Udupi Aatithya Restaurant`, `1789989649428-0d441348.png`) is a near-empty 88-byte file — confirmed this was already that size in production's own stored object metadata before this pull, i.e. a pre-existing tiny/corrupt source image from whenever the intern originally added it, not something introduced by this closeout.
- **Unhide**: all 28 new listing IDs flipped `is_hidden = false` via direct `UPDATE ... WHERE source='import' AND is_hidden AND id IN (...)` through `db query --linked` (same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as every prior batch, no service-role key used). Verified independently: re-`SELECT` confirmed all 28 `is_hidden = false` and production's overall hidden count returned to exactly 1 (the pre-existing "N Mart tea stall" row); a real anon-key REST read confirmed 3 of the 28 (Savi Sagar Grand, Brahmin Tiffins and Coffee, Sri Udupi Park) genuinely publicly readable, not just true in the privileged view.
- **The 31 rows that stayed unreviewed are now permanently marked `completed`** in the state file, same consequence as every prior partial-batch closeout — they won't resurface in a future `--push` without a deliberate state-file edit.
- Final verified state: production **360 listings, 1 hidden**; local Discovery Workbench fully reconciled before Batch 15 below was pushed.

## Discovery Workbench Batch 15 — closed out 2026-09-23 (was left partially researched)

Pushed and transferred to production 2026-09-22 (see the push/transfer record this section used to hold, now superseded by the closeout below). Checked again 2026-09-23 before touching anything — standing practice — and found only **56/100 reviewed**: 33 rows `Menu List Under 100 = Yes`, 23 `= No`, 44 still blank. Closed out anyway, same explicit choice as every prior partial batch: a blank cell purged loses nothing. Run directly at the owner's explicit request to "repeat above workbench discovery lifecycle for batch 15," reusing this section's established sequence without re-deriving it.

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via `write-updates-to-xlsx.py`; pre-checked that local's `in_progress` set matched production's 100 place_ids exactly and that none of the 100 xlsx rows already held a `Menu List Under 100` value; backed up to `output/backups/PRE-BATCH15-PULL-...bak.xlsx` first — verified field-for-field afterward, 400/400 values matched, 0 mismatches.
- **Photos**: 62 photos across 32 place_ids downloaded **one object at a time to its exact flat path**; all 62 verified byte-size-for-byte against production's `storage.objects` metadata before any deletion. This download loop ran noticeably faster than Batch 14's (a few minutes total vs. over an hour) — confirmed via the `DONE downloaded=62 failed=0` marker, not assumed.
- **Purge**: production `discovery-photos` — 62 objects deleted by **exact path list**, bucket confirmed still present (`buckets_deleted: []`, re-list showed empty). Production `discovery_batch_rows`: 100 → 0. Local's stale batch-15 copy (100 rows, all still blank, place_id set confirmed byte-identical to production's before deleting) removed directly from the local Docker stack; `workbench-state.json` hand-reconciled (all 100 `in_progress` → `completed`, 1353 completed). `workbench-sync.mjs --status` afterward confirmed 0 in-progress.
- **Import**: of 364 rows now `Menu List Under 100 = Yes`, 330 were already-imported duplicates (correctly skipped, 0 false positives), 1 was correctly flagged and excluded as duplicate-risk (the same `Udupi Vaibhava, Veg Restaurant and Party Hall` row left for a human decision since Batch 12), **33 were new**. Dry-run reviewed with extra care this time — several same-name pairs surfaced and were checked by direct coordinate comparison before approving: "KAAPI KATTE" vs "Kaapi Katte" (two new rows in the same batch, 7.26km apart — different branches); two new "Rajanna Military Hotel" entries 1.58km apart, consistent with that name already having 3+ distinct branches in production at different locations (a known common military-hotel naming pattern, not a duplicate); "Udupi Aatithya" (new) checked against both an existing "UDUPI AATITHYA" (19.25km away) and "Udupi Aatithya Restaurant" (8.73km away, imported in Batch 14) — genuinely different from both; "Udupi Kitchen" (new) checked against the existing one (20.62km away); "Sri Udupi Park Cafe" (new) checked against two existing "Sri Udupi Park" listings (2.16km and 6.24km away) — distinguishable by its "Cafe" suffix and real distance. "Park 'N' Eat" carried mojibake curly-quote characters in its source name — a source-data artifact, same category as Batch 13's garbled-name finding, not a transfer bug. "Indira Canteen" is the known government-canteen chain pattern (mechanically qualifying, institutional rather than private) already documented in this project — included, worth a human glance, not excluded. `import-excel.mjs --production --execute` inserted all 33 (`is_hidden = true`) plus 62 `listing_photos` rows/uploads in one continuous background run (~50 minutes, network-latency-bound, no shell-timeout interruption this time), zero UPDATEs/DELETEs. Production went from 360 → 393 listings and 616 → 678 photo rows.
- **Unhide**: all 33 new listing IDs flipped `is_hidden = false` via direct `UPDATE ... WHERE source='import' AND is_hidden AND id IN (...)` through `db query --linked` (same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as every prior batch, no service-role key used; IDs resolved precisely from the importer's own place_id → listing_id state rather than a timestamp-range grep, after an initial timestamp-based extraction attempt over-matched into Batch 14's own recent entries). Verified independently: re-`SELECT` confirmed all 33 `is_hidden = false` and production's overall hidden count returned to exactly 1; a real anon-key REST read confirmed 3 of the 33 (SRI GAJANANA DOSE POINT, Hangout Hut, Rajanna Military Hotel (Mahalakshmipuram)) genuinely publicly readable, not just true in the privileged view.
- **The 44 rows that stayed unreviewed are now permanently marked `completed`** in the state file, same consequence as every prior partial-batch closeout.
- Final verified state: production **393 listings, 1 hidden**; local Discovery Workbench fully reconciled before Batch 16 below was pushed.

## Discovery Workbench Batch 16 — closed out 2026-09-24 (was left partially researched)

Pushed and transferred to production 2026-09-23 (see the push/transfer record this section used to hold, now superseded by the closeout below). Checked again 2026-09-24 before touching anything — standing practice — and found only **73/100 reviewed**: 29 rows `Menu List Under 100 = Yes`, 44 `= No`, 27 still blank. Closed out anyway, same explicit choice as every prior partial batch: a blank cell purged loses nothing. Run directly at the owner's request to "repeat exact steps for batch 16 and also push batch 17."

- **Pull**: all 100 production rows read directly (`db query --linked`, read-only) and written into the real WIP xlsx via `write-updates-to-xlsx.py`; pre-checked that local's `in_progress` set matched production's 100 place_ids exactly and that none of the 100 xlsx rows already held a `Menu List Under 100` value; backed up to `output/backups/PRE-BATCH16-PULL-...bak.xlsx` first — verified field-for-field afterward, 400/400 values matched, 0 mismatches. (Practical gotcha hit while saving the pull: `supabase db query ... -o json > file` with `2>&1` mixes the CLI's stderr `Initialising login role...` banner into the file and breaks `JSON.parse` — always redirect `2>/dev/null > file`.)
- **Photos**: 59 photos across 29 place_ids downloaded **one object at a time to its exact flat path**; all 59 verified byte-size-for-byte against production's `storage.objects` metadata before any deletion (`DONE downloaded=59 failed=0`).
- **Purge**: production `discovery-photos` — 59 objects deleted by **exact path list**, bucket confirmed still present (`buckets_deleted: []`, re-list showed 0 objects). Production `discovery_batch_rows`: 100 → 0. Local's stale batch-16 copy (100 rows, all still blank) removed directly from the local Docker stack; `workbench-state.json` hand-reconciled (all 100 `in_progress` → `completed`, 1453 completed). `workbench-sync.mjs --status` afterward confirmed 0 in-progress.
- **Import**: of 393 rows now `Menu List Under 100 = Yes`, 363 were already-imported duplicates (correctly skipped), 1 was correctly flagged and excluded as duplicate-risk (the same `Udupi Vaibhava, Veg Restaurant and Party Hall` row, still awaiting a human decision since Batch 12), **29 were new**. **A near-duplicate slipped past both the importer and my first check, and was caught by a second, name-independent check**: the new Vijayanagar "Rajanna Military Hotel" (dry-run row 1413, `17th Cross Road, Vijaya Nagar`, ₹50) is **229m** from the existing "Rajanna Military Hotel Subbanna Graden" (also Vijayanagar, ₹50) — same brand, same locality, same price, different Google place_id, so probably the same restaurant listed twice by Google. My first heuristic (name-token overlap) missed it because "…Subbanna Graden" dilutes the token overlap below threshold; the importer's own `duplicateRisk()` didn't flag it either. A pure proximity scan (any new row within 600m of any existing listing or any other new row, regardless of name) surfaced it — every other hit within 600m had a clearly different name (normal Bengaluru density; e.g. food-court-style clusters), so only this one mattered. **The importer cannot skip a single Excel row** (it imports every qualifying `Yes` row), so the mitigation was import-then-hold: all 29 imported (`is_hidden = true`, 59 `listing_photos` rows/uploads, zero UPDATEs/DELETEs), then **that one listing (`2f731614-a728-40ed-8fd8-371d6dd33ff4`) was deliberately left hidden** for a human decision. Other checks accepted as genuinely different branches: the second new "Rajanna Military Hotel" (Mathikere, 4.7km from the nearest existing Rajanna; that brand now has 5+ distinct branches in production), and "New Udupi Grand Veg" (Kudlu Gate) vs existing "Udupi Grand Veg" (Bommanahalli), 2.4km apart with different addresses. **Lesson for future batches: always run a name-independent proximity check (<600m) in addition to name similarity, and treat same-brand + <300m + same price as a probable duplicate to hold back, not import-and-publish.** Production went from 393 → 422 listings and 678 → 737 photo rows.
- **Unhide**: the other **28** new listing IDs flipped `is_hidden = false` via direct `UPDATE ... WHERE source='import' AND is_hidden AND id IN (...)` through `db query --linked` (same `auth.role()`-is-`NULL`-on-a-raw-connection mechanism as every prior batch, no service-role key used). IDs were resolved precisely from each new row's place_id against the importer's own `--status` place_id → listing_id output (not a timestamp grep, which over-matched in Batch 15), and the SQL was built by a Node script (not shell `sed`/heredoc, which mangled quoting/backslashes twice this session). Verified independently: re-`SELECT` confirmed all 28 `is_hidden = false`, the held-back listing still `is_hidden = true`, and production's hidden set is exactly **2** rows ("N Mart tea stall" and the held-back Rajanna); a real anon-key REST read confirmed 3 of the 28 (Shree Sai Ram Tiffin House, traditional mulbagal dosa, AACHARYA CAFE) publicly readable **and** that the held-back listing returns `[]` to an anonymous reader.
- **The 27 rows that stayed unreviewed are now permanently marked `completed`** in the state file, same consequence as every prior partial-batch closeout.
- **Open human decision**: the held-back Rajanna listing (`2f731614-a728-40ed-8fd8-371d6dd33ff4`) — either it is the same restaurant as "Rajanna Military Hotel Subbanna Graden" (delete it, or keep it hidden), or it is a genuine second branch (unhide it). Nothing was deleted or edited.
- Final verified state: production **422 listings, 2 hidden**; local Discovery Workbench fully reconciled before Batch 17 below was pushed.

## Discovery Workbench Batch 17 — pushed and transferred to production 2026-09-24, in progress

Pushed in the same pass as Batch 16's closeout, matching the "pull, publish, purge, then push the next batch" cadence. Pre-checked both local (`--status`: 0 in-progress, 2117 remaining eligible) and production (`discovery_batch_rows`: 0 rows) before pushing.

- **Local push**: `workbench-sync.mjs --push --batch-size=100` — `"Pushed batch 17: 100 candidate(s), 0 photo(s) uploaded."` 100 rows, 100 distinct place_ids, all `batch_id = '17'`, zero overlap with the 1453 `completed`; `nextBatch` 17 → 18; 2117 → 2017 remaining eligible.
- **Production transfer**: same one-off manual INSERT method, built via a throwaway Node script from the local rows using production's exact 19-column list (re-verified via `information_schema.columns` on both sides; local's extra `no_answer_count` excluded), `chr(N)` escaping, written to a file and run with `db query --linked -f`. Verified: production holds **100 rows, 100 distinct place_ids, all `batch_id = '17'`, 0 reviewed**, with name/address/phone/latitude/longitude byte-identical to local (0 diffs across all 100 rows); listings unchanged at 422 total / 2 hidden and `listing_photos` unchanged at 737 across the transfer.
- Not yet done, by definition of "in progress": pull, purge, verify, import, unhide — check `discovery_batch_rows` directly for real progress first, as every prior closeout has reinforced.

## The established Discovery Workbench batch lifecycle

Production Workbench → Pull → verify Excel → purge Workbench → production import dry-run → import → verify → admin bulk unhide → final verification.

This is the exact sequence both Batch 3 and Batch 4 followed end to end and is the one to follow for every future batch — see the Batch 4 section above for what "verify" means at each step (field-for-field Excel/photo checks before purge; dry-run review before import; count/audit/integrity checks before and after unhide).

## Discovery Workbench — current workflow and safeguards

- **`tools/discovery/workbench-sync.mjs` is LOCAL-ONLY by design** — no `--linked`/`--production` code path exists anywhere in it, and it refuses those flags outright if passed. It only ever pushes/pulls against the local Docker Supabase stack.
- **Production's Discovery Workbench is a separate, independently-deployed instance** (its own `discovery_batch_rows` table, `discovery-workbench` Edge Function, and the deployed `discovery.html` page) that a real intern uses directly, authenticated via their own Google OAuth session — not reachable by `workbench-sync.mjs` at all.
- Getting a batch in front of the intern therefore requires a deliberate, separate, one-time production-transfer step (as done for Batches 3, 4, 5, and 7) — `workbench-sync.mjs --push` alone only ever stages a batch locally. There is no reusable script for this transfer step (checked — `tools/discovery/*.mjs` has nothing named for it); it's always been a one-off manual `supabase db query --linked` INSERT built from the local batch's own rows, using the exact same `sqlString`-style escaping `workbench-sync.mjs`/`import-excel.mjs` already use internally.
- **Every real batch has been exactly 100, not the CLI's own raw default of 50** — confirmed by counting `workbench-state.json`'s `completed` records per batch (2/3/4/5 all = 100; only the original batch 1 smoke test was smaller, at 3). Always pass `--batch-size=100` explicitly when pushing a new batch — see the Batch 6 section above for what happened the one time this wasn't done.
- Eligibility is a permanent rule on one column only: `Menu List Under 100` blank = eligible; `No`/`Yes` = permanently excluded (a decision already made elsewhere). `Number Valid` plays no role in eligibility.
- `reconcileState()` re-reads the live (local) table before every command and self-heals the local state file against it — adopts orphaned live rows, completes rows no longer live. This is what safely closed out a stale, all-blank local leftover from Batch 3's own local testing (100 rows, reconciled straight to `completed`, zero Excel writes) before Batch 4 could be pushed.
- **Never run `--pull` against a batch that hasn't been confirmed to hold genuine, current research.** Pulling writes directly into the WIP xlsx's `Number Valid`/`Menu List Under 100`/`Menu Details/Notes` columns — a stale or blank batch would silently overwrite real, already-correct data with blanks.
- **A stale LOCAL batch can outlive a batch that was already fully closed out in production.** Local and production keep independent `discovery_batch_rows` copies, so purging production's rows when closing out a batch does not touch local's own original, all-blank copy from that batch's initial local push. Before Batch 5 could be pushed, local still held Batch 4's original 100 blank rows even though the state file already had them marked `completed` — `reconcileState()` only checks `in_progress`, not `completed`, so it would have silently re-adopted them, blocking the next `--push` and, had `--pull` been run instead, overwriting valid Excel data with those stale blanks. Fix: verify and delete the stale local rows directly (after confirming no local photos exist for them) — never run `--pull`/`--pull --purge` to "clean up" a batch already known to be closed out elsewhere.
- **`supabase storage rm --linked --experimental` silently no-ops without `--yes` in a non-interactive shell** — it defaults the confirmation prompt to "No," still exits 0, and returns `{"deleted":[]}` with no error. A purge that trusts that exit code alone can report success while deleting nothing. Always pass `--yes` explicitly for a non-interactive storage delete, and always re-list the bucket afterward to independently confirm it's actually empty before purging the corresponding database rows — this is what caught it during Batch 4's photo purge.
- **`supabase storage rm <path> --recursive` on a bucket-root path deletes the bucket itself, not just its contents.** Confirmed the hard way while closing out Batch 5: `supabase storage rm ss:///discovery-photos --recursive` removed `discovery-photos` from `storage.buckets` entirely (`storage ls ss:///` afterward showed only `listing-photos`), not just the objects inside it — a real gap in the Batch 3/4 precedent this project had been treating as safe. Recovered immediately by re-running migration `0021`'s own bucket-creation `INSERT` verbatim (`id/name='discovery-photos', public=false, allowed_mime_types=['image/jpeg','image/png','image/webp'], file_size_limit=2097152`, `ON CONFLICT DO NOTHING`) — no data was actually lost since all photos in that bucket had already been confirmed backed up locally first. **Going forward: never pass `--recursive` on a bare bucket-root path when the intent is only to empty it — list the exact object paths first and delete those, or delete-then-immediately-verify-and-recreate-the-bucket like this incident did, but don't assume `--recursive` on a bucket root is content-only.**
- **Local and production `discovery_batch_rows` schemas can drift** — local's Docker stack currently has an extra `no_answer_count integer` column production doesn't have (a remnant of the parked "No Answer" call-attempt-counter feature's abandoned migration, per "Parked / uncommitted" below). Any manual production-transfer INSERT must use an explicit column list matching production's actual schema (verified via `information_schema.columns` on both sides first), never a blind `select *`.

## Discovery Workbench: current-batch progress counters (2026-09-11) — LIVE IN PRODUCTION

Small, additive UI change to the intern-facing Workbench page (`discovery.html`) — a request from the workbench-batch work above to make the intern's own progress against the current batch visible without needing the CLI's `--status` output. The header now shows all three at once, e.g. `62 / 100 reviewed · 14 / 100 no answer · 23 / 100 yes menu under ₹100`, in place of the previous `X / Y reviewed in this batch`-only line.

**Deliberately zero new state.** Before touching anything, checked what already existed: `web/src/discovery/lib/discoveryApi.ts`'s `isReviewed()` already mirrors `tools/discovery/workbench-sync.mjs`'s own `isReviewedDbRow()` exactly (by that function's own comment), and the "Reviewed" count itself already existed in `DiscoveryApp.tsx`'s header. `Candidate.number_valid` (`'Yes' | 'No' | 'No Answer' | null`) and `Candidate.menu_list_under_100` (`'Yes' | 'No' | null`) were already fully typed, populated fields. So "No Answer" and "Yes Menu Under ₹100" are just two more `.filter()` counts over the exact same `candidates` array, computed the identical way as the pre-existing `reviewedCount` — no new component, no new API call, no new column, nothing persisted beyond what already existed.

**Why this can't double-count or go stale on edit, by construction, not by extra logic**: all three counts are pure re-derivations over the live `candidates` array on every render, never separately-tracked incrementing/decrementing counters. `CandidateDetail.tsx`'s save flow already calls `onSaved(res.data)` with the full server-saved candidate — `res.data` from the Edge Function's own `update` action response, not a locally-guessed value — and `updateCandidateLocally` (in `DiscoveryApp.tsx`) replaces that one row in the `candidates` array in place. The next render just re-scans the whole (now-updated) array from scratch for all three counts, so editing a previously-saved row's `Number Valid`/`Menu List Under 100` naturally moves it in or out of each count with no special-casing needed anywhere.

**File changed**: `web/src/discovery/DiscoveryApp.tsx` only. Nothing else — `matching.mjs`/qualification logic, Excel/sync logic (`workbench-sync.mjs`), schema, `listings`, the RN app, the public web app, and the Admin panel are all untouched.

**Verification**: `cd web && npx tsc --noEmit` and `npm run build` clean, comparing two consecutive **local** builds (that comparison is valid — same `.env.local` both times, so a hash match there really does mean identical output). `npm test`: 447 tests / 403 pass / 44 fail, the 44 being the same pre-existing `adminAuth`/`discoveryWorkbench`/`workbenchSync` local-Docker-state-dependent set by name — zero regressions. No dedicated unit test exists for this component (a plain React header string), so this is code-review-level verified, not test-covered.

**A real verification-methodology mistake, caught and corrected in the same pass, worth remembering**: after deploying, `admin-*.js`'s live production hash didn't match the local build's `admin-*.js` hash, which briefly looked like an unintended admin-panel change. It wasn't — comparing a **local** build's hash against **production**'s hash is invalid for any bundle that embeds `VITE_SUPABASE_URL` (`main-*.js`, `admin-*.js`, `discovery-*.js` all do; confirmed by grepping each for the literal URL string — local embeds `http://127.0.0.1:54321`, production embeds the real project URL), since that alone guarantees the hashes differ regardless of whether any source changed. **The reliable way to confirm "X wasn't touched" is `git log --name-only <last-known-good-commit>..HEAD -- <path>` returning empty, not a local-vs-production hash comparison.** Hash comparison is still valid and useful for confirming a *specific* change did/didn't reach a bundle (e.g., grepping the live bundle for a known new string like `no answer`), and for comparing two builds/fetches from the *same* environment (two consecutive production fetches, or two consecutive local builds) — just never local-build-hash against production-fetch-hash directly.

## Latest relevant commits

- (this commit) — docs: record Discovery Workbench Batch 16 closeout and Batch 17 push+transfer in CLAUDE.md (see the dedicated sections above — 29 new listings imported, 28 unhidden and 1 probable-duplicate Rajanna deliberately held hidden, verified via anon-key REST reads; production went 393 → 422 listings, 2 hidden; Batch 17 pushed and transferred, 2117 → 2017 remaining eligible)
- `aefcae4` — docs: record Discovery Workbench Batch 15 closeout and Batch 16 push+transfer in CLAUDE.md (see the dedicated sections above — 33 new listings imported and unhidden in production, verified via anon-key REST reads; production went 360 → 393 listings; Batch 16 pushed and transferred, 2217 → 2117 remaining eligible)
- `8be6d05` — docs: record Discovery Workbench Batch 14 closeout and Batch 15 push+transfer in CLAUDE.md (see the dedicated sections above — 28 new listings imported and unhidden in production, verified via anon-key REST reads; production went 332 → 360 listings; Batch 15 pushed and transferred, 2317 → 2217 remaining eligible)
- `8f3f2d2` — docs: record Discovery Workbench Batch 13 closeout and Batch 14 push+transfer in CLAUDE.md (see the dedicated sections above — 34 new listings imported and unhidden in production, verified via anon-key REST reads; production went 298 → 332 listings; Batch 14 pushed and transferred, 2417 → 2317 remaining eligible)

- `60784be` — docs: record Discovery Workbench Batch 13 push+transfer in CLAUDE.md (see the dedicated section above — pushed and transferred to production for intern review, not yet closed out; 100 rows staged, 2517 → 2417 remaining eligible)
- `993fcf8` — docs: record Discovery Workbench Batch 12 closeout in CLAUDE.md (see the dedicated section above — 41 new listings imported and unhidden in production, verified via anon-key REST reads; production went 257 → 298 listings, 1 hidden)
- `6f47eed` — docs: record Batch 12 production transfer in CLAUDE.md (see the dedicated section above — transferred to production for intern review, not yet closed out; 100 rows staged, 2617 → 2517 remaining eligible)
- `9ce59b2` — docs: record Batch 11 closeout and Batch 12 push in CLAUDE.md (see the dedicated sections above — 23 new listings imported and unhidden in production, verified via anon-key REST reads, production went 234 → 257 listings; Batch 12 pushed locally, not yet transferred to production)
- `338d281` — docs: record Discovery Workbench Batch 11 push+transfer in CLAUDE.md (see the dedicated section above — pushed and transferred to production for intern review, not yet closed out; 100 rows staged, 2717 → 2617 remaining eligible)
- `7c42b19` — docs: record Discovery Workbench Batch 10 closeout in CLAUDE.md (see the dedicated section above — 29 new listings imported and unhidden in production, verified via anon-key REST reads; production went 205 → 234 listings, 1 hidden)
- `2982ff7` — docs: record Discovery Workbench Batch 9 closeout in CLAUDE.md (see the dedicated section above — 29 new listings imported and unhidden in production, verified via anon-key REST reads; production went 176 → 205 listings, 1 hidden)
- `bb4d66b` — fix: make content moderation importer-safe (0032, see "Security remediation (c4eae0f)" above's follow-up #1 — **applied to production 2026-09-15**, DB-only, no Edge Function redeploy needed)
- `8aaaab7` — docs: close out BudgetMap production security rollout (see "Security remediation (c4eae0f)" above)
- `c4eae0f` — fix: security remediation pass (SSRF, RLS gaps, content moderation, rate limiting, review privacy, SEO canonical, hardening) — see the dedicated section above; **production rollout COMPLETE 2026-09-15** (migrations 0026–0031 applied, `discovery-workbench`/`resolve-maps-link` redeployed)
- `c22365b` — fix(web): drop map clustering, restore plain pins, fix default zoom (see "Web map: clustering shipped then reverted same week, default zoom fixed" above; **confirmed LIVE in production** via direct bundle fetch — CSS hash byte-match against a clean local build, zero clustering strings in the deployed JS, live screenshot confirming the zoom-13 city-core view)
- `306681a` — perf(web): cluster and viewport-scope map listings (see the same section above for why this was later reverted; the viewport-scoping/bounds-containment half of this commit was **not** reverted and remains live)
- `78ad1cb` — docs: record Discovery Workbench Batch 8 push in CLAUDE.md
- `a43885f` — docs: record Discovery Workbench Batch 7 closeout in CLAUDE.md (see the dedicated section above — 27 new listings imported and unhidden in production, verified via anon-key REST read)
- `63b1500` — ci: run GitHub Actions on Node 24 to match local dev/runtime (see "CI pipeline fixed" above; **CI confirmed green**, Vercel deployment still pending explicit approval)
- `011c644` — fix(ci): stop quoting the test glob so the shell expands it consistently (see "CI pipeline fixed" above)
- `de59c03` — fix(ci): scope root tsconfig to mobile app source only (see "CI pipeline fixed" above)
- `454191c` — feat(discovery): add current-batch progress counters to the Workbench header (see the dedicated section above; **confirmed LIVE in production** via direct bundle fetch, one transient ~15s Vercel propagation gap on the first check, resolved on retry)
- `8f6a1f3` — feat(web): Add Listing photo copy/paste (contentEditable paste box, mirroring Discovery Workbench's own pattern), contextual "Can't find the restaurant?" help in the picking-dialog, and location-tabs/ⓘ-button visual polish (see "Add Listing: Photo Copy/Paste + Contextual Location Help" above; **confirmed LIVE in production**)
- `a3e7d9b` — docs: record commit hashes and confirmed production verification in CLAUDE.md
- `0164dc6` — docs: record Phase 2+3/Admin v2 production status, mobile-web fixes, and Add Listing location UX in CLAUDE.md
- `eec7f76` — fix(web): mobile browser search camera zoom, keyboard dismissal, and enter-key hint (see "Mobile Web" above; **confirmed LIVE in production** via direct bundle fetch within ~1 minute of push)
- `8c9b35e` — feat(web): simplify Add Listing location entry to paste-coordinates + add coordinate-help popover (see "Add Listing location UX polish" above; **confirmed LIVE in production**, same verification pass as `eec7f76`)
- `ee6eb2c` — feat: keep map search within current city + fix paste-link safety + POI tap-to-select (see "Location Accuracy" above; `resolve-maps-link` redeployed live; web/Vercel deploy status **now confirmed live** — re-verified via direct bundle inspection during the mobile-web fix pass above, e.g. `clickableIcons`/`poiSelectable` and the `filterByBiasDistance` guard's `6371` constant both present in the deployed bundle)
- `bb4e2bb` — fix: keep map search within current city context (see "Location Accuracy" above; web/Vercel deploy status **now confirmed live**, same re-verification as `ee6eb2c` above)
- `7603b55` — feat: replace Plausible with GA4 + Cloudflare Web Analytics (see the dedicated section above; GA4 confirmed live in production 2026-09-10, Cloudflare not yet configured)
- `baa26d6` — feat: Phase 5 — admin corrections & moderation (see the dedicated section above; deployed to production 2026-09-10)
- `726bd8c` — fix: let map-pin Review reach Edit location without losing draft state (see "Map-popup Edit Location limitation — REMOVED" above; deployed to production)
- `9c18ca0` — docs: record Phase 2/3 review+corrections status in CLAUDE.md
- `a161751` — feat: add community reviews and listing corrections (Phase 2 + Phase 3 — see the dedicated section above; deployed to production 2026-09-09)
- `758f5fc` — docs: add current project state snapshot to CLAUDE.md
- `2cdab35` — fix: parse rupee-prefixed discovery prices correctly (the `minPriceFrom()` fix behind all 32 Batch 3 imports having correct, non-quantity prices)

## Parked / uncommitted — do not touch without explicit instruction

- **The "No Answer" call-attempt counter feature is parked, unfinished, in a local git stash** (`parked: No Answer counter (unfinished, for later)`) — it is not in tracked history and not in production. Do not revive it without being explicitly asked.
- **`.claude/settings.json` carries a pre-existing, intentional local modification** unrelated to any project work above — do not commit it or otherwise resolve it without being explicitly asked.
- **Migration `0025_location_provenance_insert_persistence.sql`, plus `tests/rls.test.mjs`/`tests/locationProvenance.test.mjs`'s matching coverage for it** — see "Location Accuracy" above. Written, applied to the **local** stack only, fully verified there. Not committed, not applied to production. Do not apply to production, and do not commit either test file's current diff (it exercises this migration specifically), without explicit instruction.
- **`web/src/lib/googleMapsLink.ts`'s URL-resolution rewrite (bare-coordinate-first, `g.co`, name/address extraction, three-reason discriminated result) and `resolve-maps-link`'s matching `g.co` allowlist addition** — see "Location Accuracy" above. Uncommitted. Nothing in web's UI imports `googleMapsLink.ts` any more (superseded by the coordinates-only simplification, which **is** committed) — don't assume it's wired to anything without checking `AddListingModal.tsx`'s actual imports first.
- **The React Native mobile app's own diagnostic/fix pass** (`src/screens/MapScreen.tsx`, `src/components/ListingsMap.tsx`, `src/lib/placeRanking.ts`, `src/lib/olaMaps.ts`, plus `tests/placeRanking.test.mjs`'s extension of the "geographic sanity guard" describe block to also cover `mobile`) — see the "Mobile Web" section above for why this exists at all: a bug report was initially misdiagnosed as the RN app when it was actually mobile *web*, and this is that misdirected work. It was caught before being committed and is being **deliberately left uncommitted** — do not commit, delete, or otherwise resolve any of these five files without explicit instruction. The RN app itself was never touched by the actual (mobile-web) fix. Confirmed still uncommitted and untouched as of 2026-09-11 (checked via `git status` immediately before every subsequent commit this whole pass, every time).
